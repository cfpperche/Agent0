# 054 — Tasks

1. [x] Resolve open question #1 — **DECIDED: ask-at-Phase-0.5 with heuristic-proposed default** (NOT inferred-with-flag). Heuristic scans idea string for currency/regulation/diacritic cues + proposes default in `AskUserQuestion`; founder confirms or overrides. Stored in `.state.json.target_language`. Flag-only (no question) rejected — silent mis-language is the Vetro failure mode.
2. [x] Resolve open question #2 — **DECIDED: table format** (`| Term | Replacement | Reason | Applies to |`). Cap ≤ 20 entries per sub-section.
3. [x] Resolve open question #3 — **DECIDED: NO auto-seed from positioning Unlike-clause**. Positioning operates product-vs-product level; glossary operates copy-trap level; mechanical translation produces noise. Sub-agent identifies entries organically from concept-brief + positioning + product domain.
4. [x] Edit `templates/pipeline/13-brand/prompt.md`: added Language section requirement + Glossary section requirement with format spec + organic-identification discipline.
5. [x] Edit `templates/pipeline/13-brand/schema.md`: added Language + Glossary to required sections + dedicated `## Glossary shape` + `## Language shape` sections + Layer 1 contains-checks for both H2s + table-row anchor.
6. [x] SKILL.md Phase 0.5 NEW — heuristic + `AskUserQuestion` resolves `target_language` BEFORE Step 01; stored in `.state.json.target_language`; threaded as `{{target_language}}` substitution. Phase 4.5 updated to read state instead of re-running heuristic.
7. [x] Edit `delegation-briefs.md`: top-level preamble notes `{{target_language}}` substitution. Briefs that produce user-facing text (Step 01 concept-brief, Step 02 lo-fi mood, Step 03 functional-spec, Step 05 PRD, Step 12 gtm-launch, Step 13 brand-book) gain `Target language: {{target_language}}` CONSTRAINT line. Step 13 brief gains Glossary obligation (4-column table + organic-id discipline + no-auto-seed rule) + Language section requirement; DONE_WHEN updated. Per-stack screen-writer (Step 15) CONTEXT now points at brand-book `## Language` + `## Glossary` explicitly; new CONSTRAINT enforces `## Glossary § We don't say` term replacements with applies_to scoping; DONE_WHEN updated.
8. [ ] Verify with a dry-run on a pt-BR brand-book — confirm at least one Step 15 screen-writer produces glossary-conformant copy. **Deferred to next /product invocation**; spec 054 ships docs-only.
9. [ ] Commit: `feat(054): /product brand-book Glossary + Language — voice fidelity across downstream sub-agents`.
