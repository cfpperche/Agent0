# 084 — reminders-yaml-refactor — tasks

_Generated from `plan.md` on 2026-05-24. Work top-to-bottom. Check boxes as tasks complete. If a task reveals the plan is wrong, update `plan.md` before continuing._

## Implementation

- [x] 1. **Author Python helper** — create `.claude/skills/remind/scripts/reminders-helper.py`. ~80 LOC. Subcommands `add` / `done` / `snooze` / `prune-noop` (placeholder, not implemented). Reads YAML from `.claude/reminders.yaml` (creating with `reminders: []` if absent), mutates in-memory, writes back with `yaml.safe_dump(..., sort_keys=False, default_flow_style=False)`. Date math via `datetime.date.today() + datetime.timedelta(days|weeks=N)` (months = 30-day approximation). CLI: `python3 reminders-helper.py <subcmd> [args...]` reads/writes the file relative to `$CLAUDE_PROJECT_DIR`. ID generation: `r-YYYY-MM-DD-<slug>` where slug is first 3-5 words of context, lowercased + kebab-cased, max 30 chars. Mark `chmod +x` (executable).

- [x] 2. **Author `.claude/reminders.yaml` with 12 migrated entries** — translate each bullet in current `.claude/REMINDERS.md` to a structured entry. For each: derive `created` from `git log --follow --format=%aI -- .claude/REMINDERS.md | tail -1` heuristic (first appearance of the bullet in git) or fall back to today's date. Preserve `· due: <YYYY-MM-DD>` suffixes → `due:` field. Strip the suffix from `context`. Add `status: pending`. Bullets with explicit "See <path>" or "ver <path>" pointers can populate `links: [<path>]` opportunistically (optional, low-effort). No `check_command` on any migrated entry (none of the 12 currently carry one).

- [x] 3. **Refactor `.claude/hooks/reminders-readout.sh`** — replace body. New flow: (a) check `$CLAUDE_PROJECT_DIR/.claude/reminders.yaml` exists; if absent, emit `(no pending reminders)` inside frame and exit; (b) tool-tier: detect `yq` (Go-yq) → use `yq '.reminders[] | select(.status == "pending" or (.status == "snoozed" and .snoozed_until <= "<today>"))'`; else detect `python3` + `yaml` → invoke `.claude/skills/remind/scripts/reminders-helper.py readout` (new subcommand we add); else emit raw YAML inside frame with one-line `(yq/python3+yaml absent — install yq or pip install pyyaml for filtered readout)` advisory; (c) for each surfaced entry, emit `- [<id>] <context>` line followed by optional `  · due: <date>`, `  · check_command: <cmd>`, `  · links: <list>` sub-lines (inline per OQ-5); (d) emit closing frame. Always exit 0 (never block SessionStart). Add `readout` subcommand to `reminders-helper.py` from task 1.

- [x] 4. **Refactor `.claude/skills/remind/SKILL.md`** — full rewrite. Header frontmatter updates: `description:` reflects new subcommands; `argument-hint:` shows the 6 subcommands; bump `metadata.version` to `"0.2"`. Body sections: (a) `## Argument parsing` — unchanged logic, mention positional resolution; (b) `## Subcommand: add` — parse `<text>` + flags `--due` / `--check` / `--links`; invoke Python helper with kwargs; report `added: <id>: <context>`; validate `--due` ISO regex, refuse on invalid; (c) `## Subcommand: list` — invoke helper's `list` subcommand (or `yq` query); emit numbered list with IDs visible; print `<N> reminder(s)`; (d) `## Subcommand: done` (primary) and `## Subcommand: dismiss` (silent alias) — accept `<N|id>`, resolve position→id, invoke helper to flip status; report `done: <id>: <context>` or `dismissed: <id>: <context>`; (e) `## Subcommand: snooze` — accept `<N|id> <duration>`; validate duration regex `^[0-9]+(d|w|m)$|^[0-9]{4}-[0-9]{2}-[0-9]{2}$`; invoke helper; report `snoozed: <id> until <date>`; (f) `## Subcommand: check` — accept `<N|id>`; resolve, fetch entry's `check_command`; if absent, refuse `check: entry <id> has no check_command`; else execute the command in repo root via bash, surface stdout+stderr+exit-code verbatim; never mutate YAML; (g) Notes section: soft-delete discipline, position-resolution-at-command-time, no autonomous check execution. Cross-reference `.claude/rules/reminders.md` for full capacity description.

- [x] 5. **Rewrite `.claude/rules/reminders.md`** — full rewrite for new shape. Sections: (a) preamble + 3-bucket comparison (SESSION.md / MEMORY.md / reminders.yaml) unchanged conceptually; (b) `## Flow` — write via `/remind`, read via SessionStart hook (same); (c) `## What to write here` / `## What NOT` — unchanged content, update `REMINDERS.md` references to `reminders.yaml`; (d) `## Frontmatter schema` (NEW section) — document YAML record shape with required/optional fields; (e) `## Discipline` — replace "Deletion IS dismissal" with "Soft-delete via `status: done + completed_ts`"; document position-resolution-at-command-time; document `# OVERRIDE:` carryover for direct YAML edits if relevant; (f) `## Files` — `.claude/reminders.yaml`, `.claude/skills/remind/SKILL.md`, `.claude/skills/remind/scripts/reminders-helper.py`, `.claude/hooks/reminders-readout.sh`; (g) `## Gotchas` — keep `SessionStart` registration gotcha, add yq/Python tier gotcha, add field-order-preservation gotcha.

- [x] 6. **Delete `.claude/REMINDERS.md`** — `git rm .claude/REMINDERS.md` after verifying `.claude/reminders.yaml` carries all 12 entries (visual diff: count bullets in REMINDERS.md == count entries in reminders.yaml + each context preserved).

- [x] 7. **Cross-references sweep** — grep `.claude/rules/*.md` and `docs/specs/**/*.md` for `REMINDERS.md` and `\.claude/REMINDERS` occurrences. Update each referrer: (a) `memory-placement.md` — if it cross-references REMINDERS.md, update path; (b) `routines.md` — has a 3-row comparison table mentioning REMINDERS.md path, update; (c) `session-handoff.md` — if it cross-references, update; (d) any spec that names REMINDERS.md (likely 080 umbrella) — update to `reminders.yaml`. Single sed/edit per file; verify with re-grep.

- [x] 8. **Update umbrella 080** — in `docs/specs/080-memory-system-scale-ready/spec.md`: flip the MS-4 row's `Status` column from `pending` to `✓ shipped`; flip the `Scenario: 084 closure` checkbox under `## Acceptance criteria` from `[ ]` to `[x]`.

## Verification

- [x] 9. **Mechanical: helper script self-test** — run `python3 .claude/skills/remind/scripts/reminders-helper.py add "test entry"`; verify a new entry appears in `.claude/reminders.yaml` with `status: pending`, valid ISO `created`, derived ID matching `^r-\d{4}-\d{2}-\d{2}-[a-z0-9-]+$`; then `python3 .claude/skills/remind/scripts/reminders-helper.py done <id>` and verify `status: done` + `completed_ts` populated; clean up the test entry. **Maps to spec scenarios "structured add" + "done is soft-delete".**

- [x] 10. **Mechanical: snooze date math** — `python3 .claude/skills/remind/scripts/reminders-helper.py add "snooze test"` → snooze `<id> 7d` → verify `snoozed_until` is `today + 7 days`; snooze `<id> 1w` → verify `today + 7 days` (week alias); snooze `<id> 2026-12-31` → verify exact ISO date; snooze `<id> 7x` (invalid) → verify refusal with shape error. Clean up. **Maps to spec scenarios "snooze sets status + snoozed_until" + "snooze accepts duration shorthand + ISO date".**

- [x] 11. **Mechanical: readout filtering** — temporarily add 3 test entries (`status: pending`, `status: done`, `status: snoozed` with `snoozed_until` in future); run `bash .claude/hooks/reminders-readout.sh`; verify only the pending one surfaces; flip the snoozed one's `snoozed_until` to yesterday; re-run readout; verify both pending + past-snoozed surface; clean up test entries. **Maps to spec scenario "readout filters out done and not-yet-due snoozed".**

- [x] 12. **Mechanical: check execution** — add an entry with `check_command: 'echo "test output"'`; run `/remind check <id>` (simulated via the helper or skill body invocation); verify `test output` appears in stdout, exit code 0 reported; verify YAML unchanged after the check. Clean up. **Maps to spec scenario "check runs the probe and surfaces output".**

- [x] 13. **Mechanical: 12 migrated entries surface correctly** — run `bash .claude/hooks/reminders-readout.sh`; count surfaced entries; expect 12 (all current bullets are pending, none have due-passed status). Visually scan a sample of 2-3 to confirm context preserved verbatim. **Maps to spec static-fact "existing 12 bullets migrated manually".**

- [~] 14. **Mechanical: yq absence fallback (partial — yq not installed locally; tier 3 raw-fallback verified instead; see notes.md)** — temporarily rename `yq` (`PATH=/usr/bin:/bin bash .claude/hooks/reminders-readout.sh` or rename binary in a test env); confirm Python fallback path is hit; readout still emits filtered list. Restore PATH/binary. **Maps to OQ-3 resolution in spec.**

- [x] 15. **Mechanical: cross-references clean** — `grep -rE "\.claude/REMINDERS\.md|REMINDERS\.md" .claude/ docs/`; verify only git history references remain (no live references in `.claude/rules/*.md`, `.claude/hooks/*.sh`, `.claude/skills/*/SKILL.md`, or `docs/specs/**/*.md`).

- [x] 16. **Sanity: post-edit validator clean** — run a no-op edit through the validator path (touch a non-prod file or rely on post-edit hook in this session); confirm no `tdd-advisory:` / `lint-advisory:` / new-failure surfaces from the spec's changes. The spec itself is documentation+shell+python with no test surface — TDD advisory will fire on `.py` edit if validator stack-detects Python and no test exists; document as `tdd-exempt: helper script tested via verification tasks 9-14, no separate unit test`.

## Notes

_Anything that came up during execution that doesn't belong in plan.md but is useful for the PR description or future readers._
