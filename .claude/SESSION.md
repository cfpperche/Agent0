# Session handoff

Read at the start of every Claude Code session and updated at the end. Captures work-in-progress context that wouldn't otherwise survive between sessions.

See `.claude/rules/session-handoff.md` for the protocol (4 KB size discipline + reader-side truncation defence).

---

## Current state

**Session 2026-05-25 (late) — `cc-platform-audit` routine dispatched; one drift correction committed-pending in `.claude/memory/`.**

Routine queue (1779710401.md, queued 09:00) ran the weekly upstream-docs diff against `.claude/memory/cc-platform-hooks.md`. Upstream hooks doc <https://code.claude.com/docs/en/hooks> Hook Lifecycle table enumerates **29 events**; memo narrative claimed 32. Six narrative/frontmatter touch-ups applied; memo table itself was already 29 rows (no table edit). `MEMORY.md` regenerated by `memory-events-journal.sh` hook — index now reads "29 hook events".

Queue archived to `completed/1779710401.md`; `last-completed.json` stamped `2026-05-25T14:36:06Z`.

## WIP — resume point

**Uncommitted diff in `.claude/memory/`** (2 files, 9+/9-): the 32→29 reconciliation. Review with `git diff .claude/memory/` and commit when ready — single-purpose, atomic.

## Next steps

1. **Commit the memory drift fix** (`git diff .claude/memory/` then commit).
2. **Spec 029** (`sdd-list-in-flight`) due 2026-05-30 — check `/sdd list --in-flight` adoption; if unused, revert template change (one-liner). Reminder `r-2026-05-16-spec-029-sdd-list`.

## Decisions & gotchas

- **Drift was narrative, not table.** The memo's lifecycle table has been 29-rows for a while; only the prose ("32 events" / "8 of these 32" / "remaining 24") was stale, from the 2026-05-19 audit. Today's audit reconciled the prose to match the table and current upstream.
- **agentskills.io snapshot left alone.** Routine prompt's bonus mentioned checking `.claude/skills/skill/references/spec-snapshot.md`; deferred per `r-2026-05-17-re-snapshot-agentskills` (due 2026-08-17) — separate cadence, separate scope.
- **`memory-events-journal.sh` regenerates `MEMORY.md` mechanically.** Confirmed working end-to-end: edits to a `.claude/memory/*.md` body triggered the projection automatically; no manual `bash .claude/tools/memory-project.sh` needed.
