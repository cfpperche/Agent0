# Session handoff

Canonical runtime-neutral handoff for Agent0 sessions. Claude Code injects/nags through hooks; Codex receives the same handoff through tracked `.codex/hooks.json` after project/hook trust.

See `.agent0/context/rules/session-handoff.md` for the protocol, 4 KB size discipline, fallback behavior, and reader-side truncation defense.

---

## Current State

**▸ Session 2026-06-05 (latest) — Spec 158 `frontend-designer` skill SHIPPED (committed, NOT pushed).** Founder `/goal`: discuss-with-Codex → SDD → implement+validate → dogfood 3 `/tmp` demos. **Decision-grade `/meeting`** (claude+codex, blind openings + pressure-test, ledger 5 claims all path-anchored, `synthesis: accepted`) → graduated to **spec 158** → built the skill → **dogfooded 3 demos, all proven.** The skill is the build-time **craft loop** filling the gap between `/product` (planning, no runnable app) and the spec-155 visual-contract gate (acceptance): `create`/`refine`/`explore` modes, project-derived stack ladder (no frozen defaults), mandatory artifacted `reference-research.md`+`design-direction.md` pair, done-proof reuses `agent-browser verify-contract` (unavailable=blocker, never pass), native-only surfaces use honest labeled evidence. Canonical `.agent0/skills/frontend-designer/` (+symlinks), rule `frontend-designer.md`, CLAUDE.md block. `/skill validate` exit 0; shell test 20/20; doctor 18 ok. Dogfood (`/tmp/FD-DOGFOOD-SUMMARY.md`): A create+reused-DS web → verify-contract 6/6 PASS; B refine web → before FAIL 3/after PASS 7/7 (loop caught a real a11y bug iter 1→2); C native Expo/RN no-DS → node --test 7/7, no visual-contract claimed. Commits `…158…` (meeting, spec, impl, closeout). **One open question:** does `explore` mode earn its place (re-evaluate after first real use). **NOT pushed — human triggers push.**

---

**▸ Session arc 2026-06-05 (earlier — all on `origin/main`):** shipped **155** visual-contract-acceptance-gate (founder-triggered the lead deferred candidate; built via decision-grade `/meeting` → `/sdd` → `/squad`, 2 real defects caught by symmetric cross-review) → shipped **156** sync-exclude-od-regenerated-state (the `COPY_CHECK_SEED` ship-once/never-reconcile mechanism; motivated by a live cognixse sync) → synced the harness to **cognixse** (`74b52fc`) + **mei-saas** (`9dedd0c`), both committed/pushed, both clean (spec-156 seed killed the OD `!! customized` drift) → explored telemetry-for-the-hard-gate as **157**, ran a decision-grade `/sdd debate` with Codex, and **killed 157** (`b04501d`) as over-built/premature, recording a founder reopen-trigger for the hard gate. Details below.

---

**Spec 156 `sync-exclude-od-regenerated-state` SHIPPED + MERGED + PUSHED** (`a89048d`, `Status: shipped`). Solo SDD build (plan→tasks→impl→validate, no `/squad`). Adds a **seed-file** disposition to `sync-harness.sh` (`COPY_CHECK_SEED`): the three `/product` OD-engine-regenerated files (`od-catalog-index.json` — read at `/product` runtime; `vendor/open-design/.cache/ds-index.json`; `vendor/open-design/MANIFEST.json`) are now **ship-once, never-reconcile** — copied only when absent (cold-start preserved), left untouched once present (never compared/flagged/overwritten-under-force/deleted), and not recorded in the manifest, so a drifted consumer **self-heals in one `--apply`** (new baseline omits them). Distinct from spec-144 `COPY_CHECK_EXCLUDE` (drops entirely → would break cold-start). Test `43-seed-od-state.sh` (12 assertions), full harness-sync suite green, validated live on `cognixse` (the three now `= seed`, not `!! customized`). All 4 spec OQs resolved. Motivated by the 2026-06-05 cognixse sync drift (this session). **`cognixse` self-heals its 3 stale baseline entries on its next `--apply` — no action needed.**

_Prior this session — Spec 155 `visual-contract-acceptance-gate` SHIPPED + MERGED + PUSHED to `origin/main`_ (`356d441` plan + `03a9a54` impl; `Status: shipped`; `fb0da55..03a9a54` pushed). The spec-152 follow-up #2 (was the lead DEFERRED candidate) — built end-to-end through the full pipeline: decision-grade `/meeting` (blind commit/reveal, anchored ledger, minority report — claude+codex converged independently) → `/sdd` (spec/plan/tasks/notes, **all 6 OQs resolved, 0 deferred follow-ups**) → fail-closed `squad.json` → **`/squad`** autonomous build (6 rounds, 3 claude / 3 codex, `repair_attempts 1/4`, `ready_for_human_prod`). **What shipped (28 files, +1445/−25):** `UI impact: none|render|interaction|flow` declaration (in SDD spec template, default none); `.agent0/tools/ui-impact-detect.sh` (diff heuristic, declaration-first, suggests-never-sets); validator emits non-blocking `visual-contract-advisory:` on undeclared-UI-surface-change AND declared-UI-missing-evidence; `agent-browser.sh verify-contract` **extended past render to `interactions`+`flow` tiers** (drives act-verbs + asserts post-state; `flaky`→non-fatal warn; unavailable≠pass; backward compatible); delegation reuses the 5 fields (no 6th), `delegation-verify` checks the evidence bundle; canonical rule `visual-contract.md` + CLAUDE.md/AGENTS.md index + `/product` reconcile; offline suite `.agent0/tests/visual-contract/` (fake-bin + stateful per-route flow test). **Symmetric F2 cross-review caught + fixed 2 real defects:** verify-contract closed the browser before interaction/flow steps; validator didn't warn on declared-UI-missing-evidence. Gate 12/12; both test suites green; advisory→hard hardening explicitly OUT of 155 (future dogfood-gated spec). Meeting transcript: `.agent0/meetings/visual-contracts-sdd-delegation-gate-2026-06-05T21-29-24Z/`.

_Prior this session — TWO parallel workstreams reconciled onto `main` (now pushed):_

**(a) Spec 154 `squad-hardening` SHIPPED + MERGED** (`2392590`, `Status: shipped`) — three real defects the spec-153 `/squad` run surfaced, each recurring on every squad run: (1) the `squad.json.example` `forbidden_paths` `"secrets"` was unanchored → false-matched `secrets-scan.md` → now `(^|/)secrets?/` + `\.secrets?(\.[^/]+)?$`; (2) the session-handoff Stop-nag (`session-stop.sh`, both runtimes) fired on every `codex/claude exec` peer turn → blocked the peer into rewriting the orchestrator-owned `HANDOFF.md` → fixed at the bridge (both `codex-exec.sh`+`claude-exec.sh` export `CLAUDE_SKIP_SESSION_HOOKS=1`); (3) no non-destructive abort recovery (`rollback` discards work) → added `squad.sh resume` (re-baselines + keeps work; refuses a genuine forbidden touch without `--force`). Tests `12`/`13`/`14`, full squad suite **14/14**. Docs: `squad-contract.md` + `rules/squad.md`.

**(b) Site "Em construção" / maintenance mode COMMITTED** (`28a1e4b`, a parallel session). Founder ask: "coloque o site do Agent0 em modo Em construção — não excluir nada." Non-destructive — every page stays in source, wrapped in an `if`. **5 site files:** new `site/src/config.ts` (toggle `UNDER_CONSTRUCTION`, default `true`, env override `PUBLIC_UNDER_CONSTRUCTION=false`); new `site/src/components/Maintenance.astro` (trilingual EN/PT/ES, brand tokens, `noindex`, repo link); gated `Landing.astro` + `Doc.astro` + `pages/index.astro`. Validated `bun run build`: all 22 pages serve maintenance per-locale, zero old-content leak, `noindex` present; off-path restores the full site. **NOT pushed** → site not live yet (deploy fires only on push to `main` touching `site/**` via `.github/workflows/deploy-pages.yml`). To re-enable later: flip `true`→`false` in `site/src/config.ts`.

The two were kept as separate, non-mixed commits (scoped staging). **`main` is 6 commits ahead of `origin/main`, unpushed** — the human triggers the push (which will deploy the maintenance site).

_Prior this session — spec 153 `decouple-harness-from-playwright` SHIPPED + MERGED to `main`: the harness now depends on `agent-browser` **exclusively** — Playwright/Chrome-DevTools MCP survive ONLY as opt-in `.mcp.json.example`/`.codex/config.toml.example` templates, never a fallback the routing or any first-party skill degrades to. Flow: `/meeting` (Codex validated + hardened the scope — "coupling is in CODE, not just prose", `synthesis: accepted`) → `/sdd refine` (all 4 OQs resolved at plan time, 0 follow-ups) → **`/squad`** autonomous build (Claude↔Codex ping-pong, 4 bands), `ready_for_human_prod` at round 8, **repair_attempts 0**.
  - **What changed (27 files):** `agent-browser.sh` `route` is fail-closed (`primary`|`unavailable:{no-binary,no-chrome,mcp-removed}`; explicit cmds `run`/`verify-contract`/`audit`/`adopt` → rc 4, or rc 3 for the removed `AGENT0_BROWSER=mcp` override — **no MCP lane**); `audit` gained `--structure strict|optional` (strict = unchanged 152.1 gate; optional = h1/main advisory for fragments) + responsive 375/1280 screenshots & `scrollWidth>clientWidth` overflow fields (via a fixed internal `eval`). `/product` Phase 4 now sweeps hi-fi via `agent-browser.sh audit --structure optional` over `file://` (no `.mcp.json` seed, no HTTP server, loud skip when unavailable). `browser-auth.md` rewritten agent-browser-native (`browser-login.sh`→`adopt`; signal renamed `BROWSER_AUTH_REQUIRED`→**`BROWSER_LOGIN_REQUIRED`**; state at `.agent0/.runtime-state/agent-browser/state/<host>.json`). `serve-hifi.sh` deleted. `.browser-state/` scaffold retired (manifest/README/gitignore). Converted: `browser-primitive.md`, `runtime-capabilities.md`, `secrets-scan.md`, `doctor.sh`, `context-inject.sh`, `CLAUDE.md`+`AGENTS.md` (managed blocks, parity), `site/i18n/capacities.ts`. New `08-no-mcp-coupling.sh` grep-guard + `02`/`04` rewritten + `12` extended.
  - **Validated:** agent-browser suite **GREEN incl. live dogfood** (`run-all.sh`), harness-sync **42 PASS** (templates byte-stable — `11`/`35` green), validator + doctor (rc 0, no MCP remedy). Squad gate green; both agents propose-done. **Codex's adversarial review caught a real gap** — `adopt` was missing the fail-closed guard (AC#1 names it) → repaired + tested (`04-audit` 16/0). The 151-F2 peer-review value, realized.
  - **Predecessors 152/152.1/152.2 shipped + committed** (`f31e6b8`+`ef6b815`). Live new-mechanism auth state (`{github,x,linkedin}.json`) exists, gitignored.

_Prior — 149/149.1/150.x + 151 + 152.x shipped (harness arc + browser primitive); see git log._

## Active Work

- **Nothing in flight — clean slate.** Spec 158 `frontend-designer` SHIPPED + **PUSHED** (Agent0 HEAD `231de67`; working tree clean, 0 unpushed). **6 dogfoods** (`/tmp/fd-demo-{a..f}`, summary `/tmp/FD-DOGFOOD-SUMMARY.md`): A create+reused-DS web (6/6), B refine web (FAIL3→PASS7), C native-honesty (node 7/7), D Patudo brand landing (6/6), E explore 3-directions (zero code), F animated Three.js hero (motion proof boundary). Open questions all resolved (`explore` KEEP-narrowed).
- **`/image` → `frontend-designer` integration SHIPPED (2026-06-06).** Decision-grade `/meeting` (`.agent0/meetings/frontend-designer-image-integration-*`, Claude+Codex, ledger 6 anchored) + dogfood-proven (Patudo hero, `/image --tier=draft` beat emoji, contract still 6/6). Implemented as **opt-in guidance, no machinery**: `references/imagery.md` + craft-loop step + SKILL.md/rule. Limits: create/refine only (never explore); draft-tier auto (brand=/product); **tracked-neutral default**, never import a gitignored draft; graceful degradation (key-absent/no-credits/402/network → `image-fallback:<reason>`, build+contract pass); verify neutral path with draft disabled; redact stderr; one attempt/surface; never hard dep.
- **Consumers in sync + pushed:** `cognixse` (`fb373a7`), `mei-saas` (`1331e31`) — both carry spec-158 + the explore sharpening + the animated/WebGL done-proof section + the `/image` integration. `0 customized-refused` throughout.

## Next Actions

0. ⏭️ **NEXT TASK (founder-queued 2026-06-06) — integrate `/video` into `/frontend-designer`** (the UX/UI artist), mirroring the just-shipped `/image` integration. Same discipline: **discuss (decision-grade `/meeting` with Codex) → dogfood-prove demand → integrate as narrow opt-in guidance, graceful degradation, never a hard dep.** Key nuance to explore: `/video` has **two disjoint modes** (`.agent0/context/rules/video-gen.md`) — `code` (HyperFrames: deterministic HTML/CSS/JS→MP4, **zero inference cost, local, git-tracked source**) and `generative` (paid async fal.ai, hard `--confirm-cost-usd` gate). The `code` mode actually fits the skill's "free + local+remote" floor *better* than `/image` did → it may be the stronger/primary integration candidate (e.g. a UI micro-interaction / killer-flow walkthrough rendered deterministically), with `generative` staying the paid opt-in upgrade. Reuse the `/image` integration shape: tracked-neutral default (no video → static fallback), decorative in the contract, one render/surface, brand/marketing video boundary vs `/product`. **Not started — pick up next session.**
1. ✅ **DONE — `main` pushed** (`9164940..29bfde0`, 2026-06-05; spec 158 frontend-designer: meeting/spec/impl/closeout + dogfood notes). `explore` mode KEEP-narrowed (resolved). Skill is consumer-facing — picked up on next harness sync.
1. ✅ **DONE — `main` pushed** (`ef6b815..9840537`, 2026-06-05); GH Pages deploy fired → site live in maintenance mode. Merged branches deleted. To restore the full site later: flip `UNDER_CONSTRUCTION` `true`→`false` in `site/src/config.ts` and push.
2. ✅ **DONE — spec 155 `visual-contract-acceptance-gate` SHIPPED + PUSHED** (`fb0da55..03a9a54`, 2026-06-05). This was the lead DEFERRED candidate (visual contracts into SDD + the delegation gate); the founder triggered it and it built clean through `/meeting`→`/sdd`→`/squad` with zero open follow-ups. The "UI-producing" trigger scoping landed as the declaration-first `UI impact` field + the `ui-impact-detect.sh` heuristic.
- **DEFERRED — visual-contract HARD gate (telemetry path explored + KILLED).** 155 ships advisory-only. The "how do we get the rule-of-three evidence" question was explored as spec **157 `visual-contract-telemetry`** and **abandoned** (`3e007e9`) after a decision-grade `/sdd debate`: Claude+Codex independently judged telemetry over-built/premature (measurement for a maybe-gate, on an unreliable transcript-grep signal, single-runtime-blind; one dogfood ≠ rule-of-three). **Founder-stated reopen trigger:** *if the spec-155 advisory's pain is felt in real use (UI tasks closing without driving the UI), come back and build the HARD gate DIRECTLY — skip the telemetry.* Hard-gate design (flow-first, `SubagentStop`-blocking, `visual-contract-exempt:` override) parked in `.agent0/context/rules/visual-contract.md` § hardening trajectory; interim `jq`-on-demand recipe in `docs/specs/157-*/spec.md` § Closure; full reasoning in `157/debate.md`. **Note (2026-06-05): the cognixse dogfood already shows `verify-contract` (render+flow) run *voluntarily* against a live app — weakening the hard gate's "agents ignore it" premise.**
- `/squad` worktree-per-agent (v2) remains noted in `rules/squad.md`, unbuilt — no demand yet.

## Decisions & Gotchas

- **Visual-contract acceptance (spec 155) is a MECHANISM, advisory in v1.** Agent0 has no UI — the capability activates in consumer projects. `verify-contract` extends the existing `{required,max_console_errors}` fixture-spec with optional `interactions`/`flow` arrays (NOT a new format). `report.json` checks gained a `warn` field (`flaky` step downgrade); render-only fixtures stay backward compatible. The detector excludes `.agent0/`/docs/tests/server-langs (incl. bare `.php` — Laravel `app/*.php` false-positive; `.blade.php` matched as template first). The `.agent0/tests/visual-contract/` fake-bin stub needs `VC_FAKE_STATE` exported at _lib.sh **top level** (NOT inside `fake_bin`, which runs in a `$(...)` subshell where the export is lost). Live agent-browser suites flake when run back-to-back without `run-all.sh`'s teardown (shared global daemon) — run isolated + `agent-browser.sh reset` to confirm a real failure vs a daemon-sharing flake.
- **agent-browser daemon/port gotchas (in `.agent0/memory/agent-browser-primitive.md`):** `close --all` HANGS with no daemon (wrapper `reset` guards it); global daemon ignores `--profile`/`--state` if up (`reset` or isolated `--session`); kill by argv0 (`agent-browser-linux*`), NEVER `pkill -f agent-browser` (self-kill, exit 144). JSON envelope `{success,data,error}`; `eval --json` → `.data.result`. Pinned 0.27.1.
- **Routing is now fail-closed (spec 153 — reverses 152's permanent fallback):** `route` → `primary` | `unavailable:{no-binary,no-chrome,mcp-removed}`; explicit commands fail closed (rc 4; rc 3 for the removed mcp override). NO MCP lane in the wrapper. The `08-no-mcp-coupling.sh` grep-guard + the squad gate's inline grep prevent reintroduction.
- **`/squad` operational lessons — now FIXED by spec 154 (`2392590`):** (1) the `squad.json.example` `forbidden_paths` `"secrets"` was too broad (matched `secrets-scan.md`) → anchored to `(^|/)secrets?/` + `\.secrets?(\.[^/]+)?$`; (2) the session-handoff Stop-nag made `codex/claude exec` peer turns rewrite the forbidden `HANDOFF.md` → the bridges now export `CLAUDE_SKIP_SESSION_HOOKS=1` so a bounded subprocess never nags; (3) out-of-turn edits tripping `aborted_conflict` with no clean recovery → `squad.sh resume` re-baselines non-destructively (the manual no-op-turn workaround is no longer needed). See `rules/squad.md` § Shape + `squad-contract.md` § Recovery.
- **Removing a `.gitignore` line for a dir that still holds gitignored credential files UN-hides them** (they become untracked/stageable) — keep the retired `.browser-state/` dir gitignored until it's physically deleted.
- **Site maintenance toggle is one flag, source-preserving:** `site/src/config.ts` `UNDER_CONSTRUCTION` (default `true`). Layouts (`Landing`/`Doc`) + root `index.astro` render `Maintenance.astro` when on; the real page markup lives untouched in the `else` branch (`<!doctype html>` kept once at top, then a `{flag ? <Maintenance/> : (<html>…)}` ternary). The `check-currency.ts` prebuild guard still runs and passes regardless of the flag. Don't delete any site content — the founder constraint was explicit ("não excluir nada").
