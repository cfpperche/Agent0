# Reminders

- Fair OD re-match for spec 027 — the blind-judge result (3.87 vs 4.73) is confounded (1 OD pass vs 4 refined iterations). To measure 027 honestly: either iterate the OD run to 4 passes, or re-judge it against the first-pass baseline. See .claude/memory/od-grounding-dogfood.md § Pointers.
- Test the first real OD --bump/--apply against upstream — network-bound write-paths still untested. --check is empirically verified at the new skill location (2026-05-18 ran cleanly; upstream HEAD 34f66113 ≠ pin d25a7aaf — drift report works). See .claude/skills/product/scripts/sync-open-design.ts + docs/specs/049-od-vendor-port-to-skill/.
- Spec 029 sdd-list-in-flight — 2 weeks post-ship, check `/sdd list --in-flight` adoption; if unused, revert the template change (one line, cheap). See docs/specs/029-sdd-list-in-flight/spec.md Open Q1.  ·  due: 2026-05-30
- Write "Why I run Hermes Agent inside an Agent0-disciplined repo" post — leverage Hermes adoption peak + Słomka's "skills written without permission" critique as ammunition; positions Agent0 governance layer as the missing audit trail for Hermes users. Window closes when Hermes hype rolls off or a competitor lands the same framing first. See conversation 2026-05-17 Hermes deep-research synthesis.  ·  due: 2026-06-30
- Curate `agent0-atlas.md` (analog to hermesatlas.com — 114 repos / 12 categories curated by ksimback) when ≥10 public Agent0 forks exist. Manual curation viable at that scale; generates distribution. Until threshold: skip.  ·  due: 2026-09-30
- Add `SOUL.md`-equivalent per sub-agent to the delegation brief — research / review / writing personas have different voice rules; Hermes ships `soul.md` per agent, Honcho models humans+agents as "peers". Agent0 sub-agents currently inherit the CC default voice. Cheap design + ship; see `.claude/rules/delegation.md` § The 5-field handoff for the insertion point.
- Re-snapshot agentskills.io spec quarterly — `.claude/skills/skill/references/spec-snapshot.md` was frozen 2026-05-17. Diff upstream vs snapshot; if drift, update `scripts/validate.sh` rule set + reconcile. Deferred Open Question #5 from spec 033. See `.claude/skills/skill/SKILL.md` § Notes.  ·  due: 2026-08-17
- Re-research `/prototype` stack defaults quarterly — `.claude/skills/product/references/stack-defaults.md` was snapshotted 2026-05-17 against Next.js 16.2.6 + Expo SDK 55. Check for Next.js 17 / Expo SDK 56 / Tailwind 5 / Biome 2 / NativeWind Tailwind-4 support; re-snapshot + audit `templates/monorepo-skeleton/{next,expo}/` for staleness. From spec 034 plan Risk #2.  ·  due: 2026-08-17
- Full 23-route `/prototype` dogfood — 2026-05-17 sandbox shipped spec 034 with typecheck+lint gate empirically verified on 2-stack scaffolds + 2 dispatched screens (Next.js linear-clone + Expo habit-tracker). 20 of 23 routes were not dispatched (sandbox scope cap); mechanism is mechanical repetition. Run a fresh-session full /prototype invocation when first real user needs it — establishes the upper-bound time + token cost for a real 23-route end-to-end build.
- Revisar spec 035 dogfood window: contar missed-clarifications na janela 2026-05-17 → 2026-06-07; se ≥3 missed, scope spec 036 com UserPromptSubmit hook; senão bump spec 035 status para shipped  ·  due: 2026-06-07
- Discutir expansão full-stack do /prototype (3 caminhos analisados: /sdd new faz promoção como hoje; --scope=fullstack estende skill pra ~17 steps com BE+DB+api-contract; nova skill /promote <prototype-dir> separada que lê docs/ e gera apps/+packages/+infrastructure/). Caso motivador: ERP salões de beleza. Layout ideal proposto Turborepo + apps/booking|studio|api|mobile + packages/domain|db|api-contract|ui|config + infrastructure/. Conversa 2026-05-18; spec 037 candidate quando priorizar.
- Spec 046 dogfood gate — review next 3-5 specs scaffolded after 046; if `notes.md` is non-empty AND cited in ≥3 PRs by gate date, promote to mandatory + consider delegation-gate advisory; if empty in all, revert template + rule edits. See `docs/specs/046-sdd-in-flight-notes/spec.md` Open Q2.  ·  due: 2026-07-01
- Fix `.gitleaks.toml` starter template (spec 058 candidate) — ships with `[[allowlists]]` (plural, per-rule) which does NOT work as a global allowlist in gitleaks v8.21+. Correct syntax is `[allowlist]` (singular). Every fresh fork hits a false-positive on first commit because `.claude/rules/secrets-scan.md:104` contains synthetic AKIA fakes the scanner flags. Verified empirically 2026-05-19 bootstrapping `/home/goat/mei-saas` via sync-harness. Fix: change Agent0's `.gitleaks.toml` to `[allowlist]` + add `paths = ['''secrets-scan\.md''']` exemption. One-file fix but fork-impacting (every new fork inherits via sync-harness).
