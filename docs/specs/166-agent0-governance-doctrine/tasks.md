# 166 - agent0-governance-doctrine - tasks

_Generated from `plan.md` on 2026-06-07. Work top-to-bottom. Check boxes as tasks complete. If a task reveals the plan is wrong, update `plan.md` before continuing._

## Implementation

- [x] 1. Finalize and render the brainstorm artifact.
- [x] 2. Create the spec 166 artifact set with filled `spec.md`, `plan.md`, `tasks.md`, and `notes.md`.
- [x] 3. Add the canonical governance doctrine context rule.
- [x] 4. Add matching root entrypoint pointers in `CLAUDE.md` and `AGENTS.md`.
- [x] 5. Refresh `.agent0/HANDOFF.md` with the delivered state and next follow-ups.

## Verification

- [x] Validate the brainstorm JSON and render output.
- [x] Verify spec 166 files contain no template placeholders.
- [x] Verify the doctrine rule is reachable from both root entrypoints.
- [x] Run instruction drift checks for the managed blocks.
- [x] Run a docs-focused syntax sanity check.
- [x] Review `git diff --check` and `git status --short`.

## Notes

- This spec is documentation-only. Any future mechanism for scope admission or gate algebra should be a separate spec.
