# Session handoff

Canonical runtime-neutral handoff for Agent0 sessions.

See `.agent0/context/rules/session-handoff.md` for the protocol.

---

## Current State

- **This session (2026-06-15, Codex): fixed Agent0 validator pnpm monorepo false failure — UNCOMMITTED.** Bug came from `.agent0/validators/run.sh` assuming `pnpm test` whenever `pnpm-lock.yaml` exists. In CognixSE-style pnpm/turbo monorepos, the root package has `lint`/`typecheck`/`build` but no root `scripts.test`; tests live under package/app scripts, so the generic validator failed before the already-run proportional app validation mattered. Fix: pnpm branch now checks root `package.json` for `scripts.test`; if absent, it omits the test step and emits non-blocking `test-advisory:`. It still preserves existing typecheck behavior (`tsconfig.json` or `scripts.typecheck` only) and root repos that declare `scripts.test` still run `pnpm test`. Added `.agent0/tests/validator-js-test-script/` fixtures for root-with-test, root-without-test, and monorepo-package-test/root-without-test. Updated `.agent0/context/rules/typecheck-advisory.md` to document the new pnpm test advisory. **Validation:** `bash .agent0/tests/validator-js-test-script/run-all.sh` PASS; `bash .agent0/tests/typecheck-advisory/run-all.sh` PASS; `bash .agent0/tests/lint-validator/run-all.sh` PASS; `bash .agent0/validators/run.sh` returned `ok:true`/`no-stack-detected` with only the pre-existing spec 205 `sdd-close-advisory`.
- **This session (2026-06-15, Opus 4.8): ROOT-CAUSED the repeated Tachyon "caiu tudo" takedowns — it was a TEST killing the production tmux, NOT a wedge/OOM (in `~/tachyon`).** The maintainer insisted "I think the tests are killing the real tmux"; a codex contamination audit confirmed it. `test/unit/anchor.integration.test.ts` (which I wrote for spec 217) had an `afterAll` running `realExecutor(["kill-server"])` — UNSCOPED (no `-L`), and `realExecutor` only adds `-f /dev/null`. We run `npm test` from a shell inside a Tachyon pane, so **`$TMUX` points at the production `-L tachyon` socket** → the bare `kill-server` killed the production server → all agents died AND the test's own shell died → `vitest` got SIGKILL'd → **exit 137, which I MISDIAGNOSED as OOM/"heavy suite" for several turns** (memory was always ~9Gi free; the full suite is fast — 462 tests, ~5s). **Fixed:** `realExecutor(["-L", SOCKET, "kill-server"])` + `fs.rmSync(socketPath(SOCKET))`, matching the already-correctly-scoped sibling helpers (`tmux.real`/`verifyGate.integration`, which codex cleared). **Validated** with `$TMUX` set to production: test runs, agents stay alive, full suite 462 green, production untouched. Safe-run incantation: `env -u TMUX npx vitest run`. Durable lesson + the misdiagnosis recorded in memory `project_tachyon_test_tmux_contamination`. **Follow-up (in progress): spec 218 — strip `$TMUX` in the real-tmux test executors so the class can't recur.** (The 2026-06-14 "wedge" below was a SEPARATE, genuine incident — 58min-CPU unresponsive server — and spec 217's watchdog still addresses that.)
- **This session (2026-06-15, Opus 4.8): public-repo hygiene pass on Agent0 — UNCOMMITTED.** Context: Agent0 is a PUBLIC repo; principle established = it must NOT know/name its consumers (cognixse/mei-saas/acmeyard) anywhere in the shipped/public surface; it only knows "I'm a harness used by myself and others." Followed from spec 206 (which shipped + synced to all 3 consumers — committed `4a24cfc`/`d56c8b7`/`92d9543`/`17edd30`, done). Then ran a **leak audit** of the shipped surface and fixed two tiers (12 files, uncommitted; doctor 25 ok, ui-acceptance suite green):
  - **Tier 1 (leaks):** removed consumer names from shipped rules — `diagram.md` (dropped a cognixse+mei-saas commercial-strategy paragraph), `browser-primitive.md` ("cognixse Turnstile" → generic), `ui-acceptance.md` ("cognixse consumer" → "a real consumer project"); removed `/home/goat/anthill/...` path in `skill-rubric.md`; genericized `/home/goat/Agent0/` out of `od-sync/apply-*.md`.
  - **Tier 2 (voice):** `founder` → `maintainer` in 7 `.agent0` rules (audio, memory-placement, reminders, routines, transcribe, imagery, remind/SKILL) where it was Agent0-operator framing. **Caught+reverted** an over-broad sweep that hit 46 `/product` files — there `founder` = the product-builder PERSONA, not Agent0's operator; `git checkout` restored them. Lesson: `founder` is overloaded; no blind sed.
- **This session (2026-06-14 evening, Opus 4.8 1M) was Tachyon-repo work + a live tmux-wedge recovery — NO Agent0 code touched (only auto-memory files).** Two threads:
  1. **Recovered a wedged Tachyon tmux server (live incident).** The user reported "caiu tudo" — all agents/terminals `stopped`, tmux Server Inspector `0 sessions`, Bridge MCP disconnected. Diagnosis: the dedicated `-L tachyon` server (PID 2064880, up 2 days, **58 min CPU** on a `tail -f /dev/null` anchor) had **wedged** — process alive but unresponsive (`tmux -L tachyon ls` → "server exited unexpectedly", **ignored SIGTERM, only died on SIGKILL**). This FIRST incident was a genuine wedge (a kill-server leaves a DEAD server, not a 58-min-CPU unresponsive one). **CORRECTION (2026-06-15): the claim "NOT my npm test" was WRONG for the LATER takedowns** — those WERE my `npm test`, via the unscoped `kill-server` in anchor.integration (see the 2026-06-15 entry above). The "isolated sockets" assumption held for every real-tmux test EXCEPT that one. NOT spec 216 (never built/installed; running ext is published v0.15.0). Recovery done: SIGKILL'd the wedged server + orphan attach client (by explicit PID — `pkill -f "…tachyon"` self-matched my own shell, exit 144; use PIDs), removed the stale socket; `tmux -L tachyon ls` now clean. **Resume ledger intact** (`~/Agent0/.tachyon/sessions.json`) → nothing lost; user restarts agents from the sidebar (↻ then ▶, or resume `claude-3`) and the ext spins a fresh server.
  2. **Built Tachyon spec 216 (`role-anchoring`) in `~/tachyon` — implemented + 451 tests green, NOT committed, Tasks 9-10 remaining.** Born from a Hive RE (memory `project_hive_re`): incorporate its 3 good ideas, Tachyon-shaped. **A** role/instruction templates (`role: coder|reviewer|tester|orchestrator|custom`, composed with `instructions` at delivery — context-engineering, NOT persona; `src/roles/templates.ts`). **B** Bridge-coordination guidance appended to Bridge-spawned children (opt-out `settings.bridgeGuidance`). **C** role re-anchoring after compaction — a compaction detector (claude+codex markers, `src/anchor/compaction.ts`) rides `AttentionMonitor`; on idle-after-compaction Tachyon re-injects a role reminder via `sendKeys` (**OPT-IN, `settings.anchor.auto` default OFF** — risky live injection) + always-on manual path (`reanchor_agent` Bridge tool [21 tools now] + "Re-anchor Role" palette cmd + `.tachyon/roles/<agent>.md` durable doc). Decisions D-A…D-D locked with the user. Files: new `src/roles/templates.ts`, `src/anchor/compaction.ts`; edits to loadConfig/schema/AgentManager/AttentionMonitor/Workspace/tools.ts/Sidebar(`-ai` contextValue marker)/extension/package.json/nls + Studio (AgentForm/formLogic role dropdown). Spec: `~/tachyon/docs/specs/216-tachyon-role-anchoring/`. **Remaining: Task 9 (README docs), Task 10 (codex dueto review until SHIP), then `npx vsce publish minor`.**
  - **All 13 Tachyon dogfood pins now CLOSED** (final: p-b65563 B3+D2 = doc-only `a0882d1` — MCP has no server→agent push, `notify` is human-facing; the parent agent always pulls; cost model = payload>defs>overhead). Tachyon at v0.15.0. See memory `project_tachyon_dogfood_pin_triage`.
- **This session (2026-06-14, Opus 4.8) shipped Agent0 spec 206 (`retire-visual-contract-gate`) end-to-end — UNCOMMITTED.** Trigger: the user questioned whether the visual-contract acceptance step was dead weight. Investigation in `~/cognixse` proved it: cognixse runs a full Playwright suite in CI and the per-spec `visual-contract.json`/`agent-browser/` bundle is a strict frozen subset of the e2e that already exists (head-to-head `notifications.spec.ts` ⟷ `087/visual-contract.json`). Decision: **retire the visual contract as an acceptance artifact; UI acceptance = a green project UI test; no-runner → a `ui-runner-advisory:` to provision one (no substitute shipped); `agent-browser` stays as a dev/inspection primitive only.** A Codex agent (via Tachyon) adversarially reviewed the spec (`ship-with-changes`); its accepted points were folded in (`/tmp/codex-spec-review-out.txt`). Full SDD: `docs/specs/206-retire-visual-contract-gate/` (spec/plan/tasks/notes, status `shipped`). **Implemented (~30 files):** created `.agent0/context/rules/ui-acceptance.md` (replaces deleted `visual-contract.md`) + `.agent0/tools/ui-runner-detect.sh` (stack-neutral runner detection) + `.agent0/tests/ui-acceptance/` (42/42 pass); rewrote the validator advisory (`visual-contract-advisory:` → `ui-runner-advisory:`, moved BEFORE stack-detection so it fires on stackless repos); removed `verify_contract()` from `agent-browser.sh` and the `report.json` check from `delegation-verify.sh`; collapsed `UI impact` tiers to `none|ui`; repointed browser-primitive/delegation/spec-driven/spec.md.tmpl/frontend-designer(skill+rule)/`/product`/post-launch/spec-verify + CLAUDE.md+AGENTS.md; superseded spec 155; updated memory via `memory-maintain finalize`. **Validation:** ui-acceptance 42/42, agent-browser logic 8/8, `bash -n` all green, validator on repo `ok=true` with zero 206 advisories, `sdd-close` 206 clean, `spec-verify` 1/1 logged.
- **This session (2026-06-12, Opus 4.8) was Tachyon-repo dogfooding, NOT Agent0 code.** The user installed the published Tachyon VS Code extension and drove it live; bugs found → fixed → shipped in `~/tachyon` (separate public repo). Shipped this session: **v0.10.8** (command re-run reopens result pane), **v0.11.0** (sidebar 4 panels → 1 unified tree + inline `+`, all screenshots/site regenerated), **v0.11.1** (wedged-tmux-server detection + recovery), **v0.11.2** (live agent/terminal rename + kind-neutral item-menu labels), **v0.11.3** (sensible tmux defaults `mouse on`/`focus-events on`/`history-limit 10000` + user-configurable `settings.tmux`), **v0.11.4** (`Tachyon: Init` seeds `.gitignore` for `.tachyon/sessions.json`). All CI-green, all demand-driven. **Agent0 working tree this session: ONLY an uncommitted `.gitignore`** (added `tachyon.yml` + `.tachyon/` — local dogfood, must not ship a stack opinion to forks).
- **Competitor Overclock reverse-engineered + roadmap parked** — see memory `project_tachyon_overclock_re_roadmap`. KEY: `tachyon.yml` already supports per-agent `env:`, so Overclock's "8× cheaper" model-swap (ANTHROPIC_BASE_URL → cheap providers) is a 5-line config today; gap is UX/docs. Roadmap (E1/F1/E2/F2/F3) awaits the user's multi-day dogfood decision.
- **Spec 205 (`product-honesty-and-fit`) COMMITTED `ae6bd8f`, closed `5b89c3c` (Claude, Fable 5, 2026-06-12).** Six maintainer-approved changes to `/product`, now v0.6.0 (state v6, refuses v5): (1) Step 03 Assumption Register replaces synthetic problem-validation interviews (bets table + riskiest assumption + cheapest test + abandon signal — advice-only); Step 01 market sizing reframed as hypothesis with ranges; OST/PRD consumers carry confidence tags. (2) Quality judge batched per phase — 4 calls/run instead of 17 — on a PROVISIONAL sonnet(P1/P3)/opus(P2/P4) mix; measurement protocol in `quality-judge.md`; reminder filed (`r-2026-06-12-run-the-spec-205-judge`). (3) New `gate_concept` after Step 01 (continue/adjust/abort, judge-free) + phase gates open with a 3-5 item distilled review agenda. (4) Steps 10-12 open with a `**Pre-validation projection.**` blockquote (Layer 1 anchored); cost figures as ranges. (5) Product-form awareness — Step 01 declares `screen-app|headless-service|cli|bot|embedded` → `.state.json.product_form`; new `references/product-forms.md` drives Steps 02/07/14/15 variants; screen-app path unchanged. (6) New `scripts/staleness-check.{ts,test.ts}` — post-run advisory for stale downstream artifacts after upstream hand-edits. **Validation:** skill validator exit 0 (fixed a PRE-EXISTING rule1 failure: `argument-hint` was never valid YAML), staleness tests 11/11, product-overwrite 5/5, full battery 44/44, spec-verify pass logged. Spec status `shipped-partial` — residual = the judge-mix measurement run on the next real dogfood. Spec numbering note: 186-204 were Tachyon's (deleted at graduation) → this spec is 205.
- **Rule-corpus cleanup executed + synced to all 3 consumers (Claude, Fable 5, 2026-06-10).** Agent0 pass: (1) register-split of the 4 largest rules — operative text stays, design memory moved verbatim to `.agent0/memory/{harness-sync,delegation,memory-placement,secrets-scan}-maintenance.md` (one-directional linkage, no memory pointers in rules); (2) citation sweep across ALL shipped rules: ~70 spec-number/docs-path/memory-pointer leaks removed, facts kept. Corpus 417→384 KB; global leak grep returns zero; 44/44 suites + drift + doctor 25 ok + harness-tests CI green. **Consumer sync DONE + PUSHED, one at a time, each user-approved, each audited:** mei-saas `4e81afd` (doctor 24 ok / 1 advisory = pre-existing project-core review), acmeyard `d903c91` (25 ok / 0), cognixse `19c8447` (25 ok / 0). Each: 52 files (15 copied, 34 updated, 2 removed), **0 customized-refused / 0 overwritten**, the 2 relocated governance rules auto-deleted, zero citation leaks downstream, P8 `shipped integrity` debuted in the field (53 executables verified vs baseline per consumer).
- **CORRECTION to record — managed-block sync is NOT append-only.** Prior handoff/memory claimed the CLAUDE.md managed-block merge "adds sections, never removes" → manual deletion of the stale `## Agent0 governance doctrine` section would be needed per consumer. **False:** the region between `AGENT0:BEGIN`/`AGENT0:END` is replaced *wholesale* each sync (consumer's own CLAUDE.md documents this), so the section auto-removed cleanly in all 3, markers intact. **No manual cleanup was needed anywhere.** The relocated rule files are whole-file synced → also auto-deleted (not manual). Update `propagation-hygiene` memory's "append-only / known limitation" paragraph if it still claims otherwise.
- **Spec 185 (`harness-evolution-program`) shipped + closed (Claude, Fable 5, 2026-06-09→10).** The 8-point external harness review ran one detailing round per point; all dispositioned: **executed** P4 (rule-corpus discipline adopted: audience test "consumer-facing → rule, else memory or gate"; 3 rules relocated to memory, corpus 39→36, one sync special-case deleted), P7 (CI: `.agent0/tests/run-all-suites.sh` + `.github/workflows/harness-tests.yml`, green in ~3min — first runs caught 4 real defects: stale test, rg dependency, missing skip-guard, SC2034), P8 (doctor `shipped integrity` section verifying executable shipped surface vs sync baseline, advisory-only, suite 5/5); **decided** P2 (Agent0 = personal lab + public showcase; no adoption machinery; reopen on first external interest); **killed** P5 (hook chains measured 7ms/46ms — non-problem), P6 (bash kernel rewrite buys nothing for the operator); **deferred** P1 (evidence bundle — no users yet; ephemerality finding + reopen triggers recorded), P3 (multi-runtime parity stands; Codex re-tier analysis preserved). Full record: docs/specs/185-harness-evolution-program/{tasks,notes}.md.
- **⚡ Tachyon GRADUATED to its own public repo (2026-06-10) and was removed from Agent0.** Incubated here as `packages/tachyon/` across specs 186–204 (v1 + F1/F2/F3/F4/F12/F13/F14/F15/F16/F17/F18/F19/F20/F21/F9; F22 split-panels shipped then hidden behind a feature flag). Split out with **full git history** (`packages/tachyon/` → repo root + `docs/specs/186–204`) via `git filter-repo` into **[github.com/cfpperche/tachyon](https://github.com/cfpperche/tachyon)** (public, MIT, CI green = typecheck+build+188 unit, GitHub Pages live at cfpperche.github.io/tachyon, last shipped 0.4.6). The package, its 19 specs, and the `.vscode/launch.json|tasks.json` dev configs were then deleted from Agent0 (tracked files git-rm'd; Agent0's git LOG still retains all the work). Dogfood lives outside both repos at `~/tachyon-demo*`. **Agent0 no longer hosts any product code — it is purely the governance/harness template again.**
  - Known follow-up for the Tachyon repo (not Agent0's concern): one-shot command exit-code capture is tmux-version-sensitive (3.4 leaves `pane_dead_status` empty for instant-exit panes; 3.6 populates it) — bump documented min tmux to 3.6 or harden CommandRunner. The xvfb integration suite is a local gate there, not in CI.

## Active Work

- **Agent0 pnpm validator fix is implemented + validated, UNCOMMITTED.** Changed files: `.agent0/validators/run.sh`, `.agent0/context/rules/typecheck-advisory.md`, `.agent0/tests/typecheck-advisory/04-pnpm-advisory-fires.sh`, and new `.agent0/tests/validator-js-test-script/`. Next session should review/stage/commit if the user approves; no CognixSE product code was touched.
- **Tachyon spec 216 (`role-anchoring`) — finish + ship (in `~/tachyon`, separate repo).** Implementation done, 451 unit tests + typecheck green, NOT committed. **Next: Task 9** (README — roles section, Bridge guidance, the opt-in anchor + the claude/codex-only runtime gap), **Task 10** (codex dueto adversarial review, Part C/injection-timing gets the heaviest scrutiny, iterate until SHIP), then `npx vsce publish minor` (auto-bumps/commits/tags; push + tag separately; CI green first). Tachyon commit trailer is `Co-Authored-By: Claude …` per its repo.
- **Tachyon backlog finding (pin it when the Bridge MCP is back):** the wedged-server auto-recovery (v0.11.1) has a GAP — it watches pane-death, not server RESPONSIVENESS, so a server that goes alive-but-deaf ("exited unexpectedly", ignores SIGTERM) is never auto-cleared. Proposed fix: a periodic health-probe (`tmux -L tachyon ls` with a short timeout → on timeout/"exited unexpectedly", `kill -9` + recreate).
- **Spec 206 is fully implemented + validated but UNCOMMITTED** (we are on `main`; I do not commit Agent0 without an explicit ask). When the user says go: branch off `main` first, stage + commit in SEPARATE Bash calls (secrets-preflight blocks compound `git add && git commit`), commit trailer per `CLAUDE.md`. The full change set is the ~30 files above + `docs/specs/206-*` + deleted `visual-contract.md`/`tests/visual-contract/`.
- **Two pre-existing items deliberately NOT touched by spec 206** (not followups of this work): (1) `.agent0/tests/agent-browser/13-dogfood-adopt.sh` fails — it is a LIVE test needing interactive CDP login, impossible headless; `adopt` was untouched. (2) `sdd-close-advisory` for spec **205** — its unchecked acceptance box is the intentional `shipped-partial` residual (judge-mix measurement, reminder `r-2026-06-12` filed); checking it would be false.
- **Public-repo hygiene — Tier 1+2 done (uncommitted, 12 files); two micro-decisions pending the user's go, then discussions 3 & 4.** I recommended:
  - **Item 1 (`Acme Yard` in `product/SKILL.md:278` + `validators/run.sh:156`):** LEAVE — fictional placeholder (the consumer `acmeyard` was instantiated FROM the example, not vice-versa); over-scrubbing fictional placeholders is over-correction. (User to confirm.)
  - **Item 2 (`runtime/od-sync/*.md` logs):** `git rm --cached` + gitignore — regenerable verbose logs; durable audit (vendor SHA + check timestamps + history) already in `MANIFEST.json`; kills the public path-leak AND the needless consumer-shipping at once; supersedes spec 027/049's "commit daily reports." Generator's absolute-path bug (`SKILL_ROOT` from `import.meta.url`) then becomes cosmetic. (Awaiting user go.)
  - **Discussion 3 (deferred):** 84 NON-shipped-but-PUBLIC files (`.agent0/memory/**`, `HANDOFF.md`, `docs/specs/**`) name consumers — decide leave / scrub / move-memory-out. Git HISTORY already holds all old leaks publicly; `filter-repo` rewrite is nuclear, out of scope unless asked.
  - **Discussion 4 (deferred):** make Agent0 a clean PUBLIC VERSIONED upstream consumers PULL from (tags/releases + generic consumer-side "am I behind?" check; multi-repo fan-out = operator's PRIVATE tooling, never in the public repo). Feeds the sync-harness-vs-copier audit (Codex verdict: split-decision — keep the semantic-merge contract; weigh repackaging distribution as a versioned CLI vs Agent0's pure-Bash stack-neutrality; `/tmp/audit-sync-harness-vs-copier.md`).
- **(Prior session, 2026-06-12) Tachyon dogfood — item 1 pending:** the "needs you" attention toast fires even when the agent's terminal is already the focused editor tab. Diagnosed fix (in `~/tachyon`): add `Terminals.isActive(agent)` (`byAgent.get(agent) === vscode.window.activeTerminal`) and gate `Workspace.ts` toast `if (shouldToast && state === "needs-input" && !this.terminals.isActive(agent))`; the sidebar badge stays (it's fired before the gate). NOT started — about to implement when the session resumes.
- **Uncommitted Agent0 `.gitignore`** awaits the user's commit decision (I do not commit Agent0 without an explicit ask). It adds `tachyon.yml` + `.tachyon/`.
- Two more parked Tachyon threads: the **Overclock roadmap** (see memory) and nothing else in flight on the Agent0 side.

## Next Actions

- **For the pnpm validator fix:** if the user says to ship it, run `git diff`, optionally rerun `bash .agent0/tests/validator-js-test-script/run-all.sh && bash .agent0/tests/typecheck-advisory/run-all.sh && bash .agent0/tests/lint-validator/run-all.sh`, then stage and commit in separate Bash calls. After merge/push, sync the harness to CognixSE so `.agent0/validators/run.sh` no longer fails solely because the root monorepo lacks `scripts.test`.
- **Verify the Tachyon recovery landed:** confirm with the user that after ↻ + ▶ (or resume `claude-3`) the agents come back and a fresh `tachyon` server exists (`tmux -L tachyon ls` shows live sessions). If a wedge recurs, the fast clear is: `kill -9` the `-L tachyon` PIDs (NOT `pkill -f` — it self-matches), `rm /tmp/tmux-$(id -u)/tachyon`, then start an agent.
- **Finish Tachyon spec 216** (Task 9 docs → Task 10 codex dueto → `vsce publish minor`). See Active Work.
- **Commit spec 206** when the user approves (branch off `main`; separate add/commit calls). Then optionally `sync-harness.sh` the change to consumers (mei-saas/acmeyard/cognixse) — note `ui-acceptance.md`/`ui-runner-detect.sh` are new files and `visual-contract.md`/`tests/visual-contract/` are deletions; the rule files customized per-consumer (Notes sections) will flag for manual reconciliation.
- ~~Implement Tachyon item 1 (toast-focus gate) → v0.11.5~~ **DONE long ago** — Tachyon is at v0.15.0; all 13 dogfood pins closed. The parked thread now is the **Overclock model-swap roadmap** (E1→F1), to revisit after the user decides.
- **Decide: commit the Agent0 `.gitignore`?** (user's call.)
- **Next `/product` dogfood run:** execute the judge-mix measurement protocol (`quality-judge.md § Measurement protocol`) — reminder `r-2026-06-12-run-the-spec-205-judge` tracks it; append the run to `.agent0/memory/product-pipeline-empirical-baseline.md`.
- **CI is live:** every push touching harness paths runs all 44 suites (`harness-tests` workflow). If it goes red, fix before continuing — that's the operator-quality bar the maintainer set.
- **Consumer sync — DONE (mei-saas `4e81afd`, acmeyard `d903c91`, cognixse `19c8447`, all on origin/main).** No follow-up; no manual cleanup was required.
- ~~Audit item 2~~ **DECIDED 2026-06-10:** spec 171 closed as abandoned — the index + on-demand-read context model (no prompt-time injection) is the declared-final design; pause language removed from runtime-capabilities/harness-sync/startup-brief; reopen trigger = a second silent-substitution incident.

## Decisions & Gotchas

- **Validator pnpm test behavior:** root `pnpm test` is now manifest-as-intent, not inferred from `pnpm-lock.yaml`. In pnpm monorepos, package/app tests are not auto-discovered by the generic validator; the validator emits `test-advisory:` and leaves proportional package/app commands to the spec/task proof path. If no root validation stages remain after omissions, the JSON command is `true` and `ok:true` when advisories are the only signal.
- **Agent0 is PUBLIC and must not know its consumers.** The shipped/public surface (rules, skills, tools, validators, hooks, CLAUDE.md/AGENTS.md) must never name cognixse/mei-saas/acmeyard or carry private `/home/<user>/` paths or commercial strategy. Agent0 only knows "I'm a harness used by myself and others." Distribution is PULL-based (Agent0 publishes; consumers pull); a registry/fan-out of consumers lives in the OPERATOR's private tooling, never in the public repo. Audit command: `git grep -nI -iE 'cognixse|mei-saas|acmeyard' -- <shipped paths>` (exclude `runtime/`).
- **`founder` is an overloaded word — never blind-sed it.** In `.agent0/context/rules/*` it means the Agent0 operator (→ `maintainer` is right). In `/product` it means the product-builder PERSONA (the user running `/product`) — leave it. In legal/roadmap templates it's domain ("startup founders") — leave it. A repo-wide `s/founder/maintainer/` wrongly hit 46 `/product` files this session and was reverted.
- **UI acceptance model (spec 206):** acceptance of built UI = a GREEN PROJECT UI TEST covering the surface, never a frozen `agent-browser` bundle. `agent-browser` is a dev/inspection primitive only (`verify-contract` is GONE). When a UI surface changes but no UI test runner is declared, the validator emits `ui-runner-advisory:` — the harness REQUIRES the runner, it does not ship a substitute. Runner detection = `.agent0/tools/ui-runner-detect.sh` (package.json `test:e2e`-style script / playwright·cypress·wdio·nightwatch config / `.agent0/ui-test.json` override). The advisory is computed BEFORE stack-detection in `run.sh` (else stackless-UI repos skip it). `UI impact:` is now `none|ui` (tiers collapsed). `/product`'s DESIGN-TIME visual contract (screen-atlas/fixture-spec) SURVIVES as test-writing input — distinct from the retired acceptance gate. Canonical rule: `.agent0/context/rules/ui-acceptance.md`.
- The secrets-preflight hook blocks compound `git add … && git commit …` — stage and commit in separate Bash invocations.
- **Agent0 is a portable governance/evidence harness, not a product host.** Tachyon's graduation (2026-06-10) restored that: `packages/` is gone; if a future product is incubated here it follows the same arc — built in `packages/<name>/`, never touching `.agent0/`/`.claude/`, then split out with `git filter-repo` when it earns its own repo.
- **Wedged tmux server failure mode (live, 2026-06-14):** a long-lived `-L tachyon` server can go alive-but-unresponsive — `tmux -L … ls` returns "server exited unexpectedly", the process ignores SIGTERM (only SIGKILL works), all panes are effectively dead. Distinct from a crash (process gone). The resume ledger (`.tachyon/sessions.json`) survives → no data loss; clearing the socket + restarting agents recovers fully. **Gotcha within the gotcha:** `pkill -f "…tachyon…"` matches your OWN shell's command line (the pattern text) and SIGTERMs the running script (exit 144) — kill wedged tmux by **explicit PID**, never `pkill -f`.
- **Tachyon work happens in `~/tachyon` (separate repo), not here.** This Agent0 session only touched `.gitignore`. Tachyon ships via `npx vsce publish <patch|minor>` (auto-bumps + commits the version), commit trailer `Co-Authored-By: Claude Fable 5`. The live tmux server socket is `-L tachyon`; `mouse on` was set on it at runtime this session (the v0.11.3 code now seeds it from boot — already-running agents need a restart to pick up focus-events/history-limit).
