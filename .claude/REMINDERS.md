# Reminders

- Explore CC hooks underused em Agent0 — especialmente UserPromptSubmit pra detectar prompts ambíguos/sem-contexto antes de virarem turno desperdiçado. Outros candidatos: Setup, UserPromptExpansion, Notification, PostMessage. Ver `.claude/memory/cc-platform-hooks.md` § The 29 events pra inventário completo; cruzar com `.claude/settings.json` hooks registrados pra achar os gaps. Avaliar 1-2 que fechem gap real e prototipar.  ·  due: 2026-05-13
- Supply-chain block gap: parent-edit-manifest + bare-install pattern bypasses both layers. Bash hook audits bare `bun install` (no args) as `skip-not-install` per design; PostToolUse Edit advisory is sub-agent-only. Result: parent who edits package.json devDeps then runs `bun install` adds deps with zero block + zero advisory. Caught via shrnk-mono spec 013 dogfood 2026-05-12 (the `# OVERRIDE:` marker on `bun install` was defensive but turned out unnecessary). Document in `.claude/rules/supply-chain.md` § Gotchas as known coverage gap; consider whether parent Edit advisory is worth adding (current actor-split intentionally exempts parent — needs deliberate decision).  ·  due: 2026-05-19
