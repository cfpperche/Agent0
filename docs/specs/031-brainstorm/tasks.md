# 031 — brainstorm — tasks

_Generated from `plan.md` on 2026-05-16. Work top-to-bottom. Check boxes as tasks complete. If a task reveals the plan is wrong, update `plan.md` before continuing._

## Implementation

### Setup (1 task)

- [x] 1. **Scaffold skill dirs + gitignore** — create `.claude/skills/brainstorm/`, `.claude/skills/brainstorm/templates/`, `.claude/skills/brainstorm/references/`. Append `.claude/.brainstorm-state/` to `.gitignore` (verify the line isn't already present). DoD: `ls .claude/skills/brainstorm/{templates,references}` succeeds; `grep -q '^\.claude/\.brainstorm-state/' .gitignore` succeeds.

### Reference data (1 task)

- [x] 2. **Write `references/techniques.md`** — one section per lens (SCAMPER, Six Thinking Hats, Reverse Brainstorm, Crazy 8s). Each section contains: short description, when-to-apply heuristic (the trigger the skill uses to suggest it), Claude's protocol for the lens (questions to ask, how to walk the existing ideas through the lens, what to capture into the state JSON). Length budget: ~30–60 lines per lens, ~200 lines total. DoD: file exists; all 4 lenses present; each has the 3 sub-sections (description, when-to-apply, protocol).

### HTML template (4 tasks)

- [x] 3. **`render.html.tmpl` skeleton** — full HTML5 doc with `<head>` (meta charset, viewport, title `{{TOPIC}} — brainstorm`), inline `<style>` with vanilla CSS (no Tailwind), `<body>` containing: header (`{{TOPIC}}` + `{{TIMESTAMP}}` + count line `{{IDEAS_COUNT}} ideas | {{QUESTIONS_COUNT}} open questions | {{LENSES_COUNT}} lenses applied`), `<nav>` tab bar (placeholder for tab buttons), tab-content container (empty), footer (placeholder for export button). Include the two CDN `<script>` tags at end of `<body>`: `markmap-autoloader` and `mermaid` (both pinned to specific versions, not `@latest`, to avoid surprise breakage). Include inline `<script>` for tab-switching JS. DoD: opening the unsubstituted template in a browser (via `python3 -m http.server`) renders without console errors; tab switching works on placeholder buttons.

- [x] 4. **Exploration tab body** — first tab, always present, default active. Contains four blocks: (a) Mindmap — `<div class="markmap"><script type="text/template">{{MINDMAP_MARKDOWN}}</script></div>` (markmap-autoloader picks this up); (b) Kanban cards — 4 columns (`easy`, `risky`, `wild`, `unknown`), each column populated by iterating `{{IDEAS_JSON}}` via inline JS at page load; (c) Open questions — `<ul>` populated from `{{QUESTIONS_JSON}}`; (d) Quotes — `<blockquote>` list from `{{QUOTES_JSON}}`. DoD: with sample data substituted into placeholders, the four blocks render in the browser; kanban columns visually distinct (CSS); markmap mindmap displays with collapse/expand.

- [x] 5. **Lens tabs + Timeline tab** — (a) Lens tabs: one tab per entry in `{{LENSES_APPLIED_JSON}}` (rendered server-side by Claude during `done`, since Claude writes the final HTML directly), each tab body iterates that lens's derived ideas + critiques + new questions. Use the same kanban-card pattern as Exploration but visually distinct (badge or color hint per lens). (b) Timeline tab: mermaid `timeline` block populated from `{{TIMELINE_MERMAID}}` showing turn-by-turn chronology with brief turn summaries. DoD: with a sample state containing 2 lenses, 3 tabs render correctly (Exploration + Lens-A + Lens-B + Timeline); timeline renders as mermaid timeline; clicking tabs swaps the visible body.

- [x] 6. **Footer + Copy-as-markdown button** — footer contains a single `<button id="copy-md">Copy as markdown for /sdd new</button>` plus a `<script>` block embedding the full state JSON as `const STATE = {{STATE_JSON}};`. Button click handler synthesises a markdown block (topic h2, ideas grouped by tag with their text, open questions list, lens summaries) and calls `navigator.clipboard.writeText(...)`. On success: button text flashes to `Copied ✓` for 2s. On failure (clipboard API denied — e.g. `file://` origin): show inline `<textarea>` with the markdown preselected so the user can manually Ctrl-C. DoD: served via `python3 -m http.server`, clicking the button copies to clipboard; opened via `file://`, fallback textarea appears with the markdown preselected.

### SKILL.md orchestration (5 tasks)

- [x] 7. **`SKILL.md` skeleton + argument routing** — frontmatter (`description`, `argument-hint: <start "<topic>" | list | resume <slug-or-filename> | done>`), preamble (1 paragraph: what this skill does + sits-before-`/sdd refine` in the ideation pipeline), `## Argument parsing` section mirroring `/sdd`'s pattern (split `$ARGUMENTS` on first whitespace, treat the rest as topic for `start`), subcommand routing table, `## Unknown subcommand` fallback. DoD: invoking `/brainstorm` with no args prints a usage hint; invoking `/brainstorm start "test topic"` correctly identifies subcommand=`start` and topic=`"test topic"` (parse handles the quoted form).

- [x] 8. **`SKILL.md` § subcommand `start`** — define behaviour: validate topic non-empty; slugify topic (kebab-case, max 40 chars); generate ISO timestamp; create `.claude/.brainstorm-state/<slug>-<ts>.json` with the initial structure `{topic, started_at, ideas: [], questions_open: [], quotes: [], connections: [], lenses_applied: [], turns: []}`; print confirmation + open the session with **one** grounding question (no technique chosen up-front). Include the explicit instruction: "do NOT propose solutions in opening — ask, capture, classify, repeat." DoD: this section, read in isolation, gives Claude enough to perform the scenario from spec.md § *start a free-form session* without further clarification.

- [x] 9. **`SKILL.md` § capture loop + checkpoint** — define: (a) classification taxonomy — every substantive user turn yields zero or more entries in `{ideas, questions_open, quotes, connections}`; tag rubric for ideas (`easy` = clearly doable; `risky` = depends on assumption; `wild` = high payoff if true; `unknown` = needs more info). Examples for each. (b) State-update protocol — after each substantive turn, use `Edit` (not `Write`, to avoid race-y rewrites) to update the JSON file; append a turn-summary to `turns[]`. (c) Checkpoint cadence — every 5–7 substantive turns, emit one line with counts and offer 3 branches: *continue free | apply lens | `/brainstorm done`*. DoD: this section gives Claude unambiguous rules for what to write where, when to checkpoint, and what the 3-branch UI literally says.

- [x] 10. **`SKILL.md` § lens application + suggestion heuristic** — (a) Lens application protocol — when user says "apply SCAMPER" / "Black Hat" / "reverso" / "Crazy 8s" (or accepts a suggestion): read the matching section of `references/techniques.md`, walk existing `ideas[]` through the lens following the protocol, capture derived ideas (tagged) + critiques + new open questions into state, append lens name to `lenses_applied[]`. (b) Suggestion heuristic — at checkpoint time, if (≥10 ideas AND zero lenses applied) OR (all ideas share one tag), name a specific lens with the reason. Example sentences to use. (c) Composability v1: **no** — one lens at a time; chain manually by re-invoking after the first completes. DoD: this section, paired with techniques.md, lets Claude apply a lens end-to-end without re-asking the user how.

- [x] 11. **`SKILL.md` § `done` + `resume` + `list`** — (a) `done`: finalise state (set `ended_at`, `state: "done"`); render the HTML by reading `templates/render.html.tmpl`, substituting all `{{...}}` placeholders against the state JSON (including pre-rendering markmap markdown for the mindmap, mermaid timeline string for the Timeline tab, and lens-tab HTML for each entry in `lenses_applied[]`); write `.claude/.brainstorm-state/<slug>-<ts>.html`; print absolute path + the copy-paste command `python3 -m http.server 8765 -d .claude/.brainstorm-state` + the URL `http://localhost:8765/<filename>`. (b) `resume <slug-or-filename>`: find the matching `.json`, refuse with a list of candidates if ambiguous, load state, emit counts summary, continue. (c) `list`: enumerate `.claude/.brainstorm-state/*.json`, one line each: `<topic-slug>  <ISO-ts>  N ideas  M lenses  <state>`. DoD: each subcommand has a self-contained sub-section in SKILL.md whose protocol is unambiguous.

### End-to-end validation (3 tasks)

- [x] 12. **Smoke test — start to done** — open a fresh Claude Code session; invoke `/brainstorm start "test-spec-031"`; contribute 3 ideas + 1 open question across 3-5 turns; observe checkpoint behaviour; apply one lens (SCAMPER); invoke `/brainstorm done`; serve the rendered HTML via `python3 -m http.server 8765 -d .claude/.brainstorm-state`; open in browser; verify: tabs render (Exploration + SCAMPER + Timeline), markmap mindmap displays + collapses, kanban cards distribute by tag, Copy-as-markdown button copies a sensible markdown block. Record any defects as plan-drift in `plan.md`.

- [x] 13. **Smoke test — resume** (mechanical-only this session: glob + JSON parse + state field validated; full end-to-end resume in a fresh CC session deferred to user) — close the chat after Task 12 mid-session (i.e., before `done`); start a new chat; invoke `/brainstorm resume test-spec-031` (or the file slug printed in Task 12); verify the skill summarises current counts and continues from where it left off, without re-asking what was already captured.

- [x] 14. **Sync-harness check** — confirmed: SKILL.md + templates/ + references/ all listed by `--check`; `.brainstorm-state/` not in scope — run `bash .claude/tools/sync-harness.sh /tmp/agent0-fork-sync-check --agent0-path=. --check 2>&1 | head -50` against a throwaway dir to confirm the new skill files (`SKILL.md`, `templates/`, `references/`) are picked up by the sync manifest and that nothing in the `.brainstorm-state/` runtime path is being synced. If the manifest doesn't already cover `.claude/skills/<name>/templates/` and `.claude/skills/<name>/references/` recursively (it should — verify), open a follow-up sync-harness fix as a separate task in this tasks.md and continue.

### Spec close-out (1 task)

- [x] 15. **Flip spec status + update SESSION.md** — change `spec.md` header from `**Status:** draft` to `**Status:** in-progress` once Task 1 lands, then to `**Status:** shipped` once all `## Acceptance criteria` boxes are checked and Tasks 12-14 pass. Update `SESSION.md` § *Current state* with the spec-031 outcome and remove it from § *Next steps*. Commit the spec, plan, tasks, skill files, and `.gitignore` change as one logical commit (or two: skill files first, then verification).

## Verification

_Each check maps to one or more spec.md acceptance criteria. Run after Tasks 1-11 complete; gate on these passing before declaring the spec shipped._

- [x] **V1**: `ls .claude/skills/brainstorm/SKILL.md .claude/skills/brainstorm/templates/render.html.tmpl .claude/skills/brainstorm/references/techniques.md` returns all three paths — covers spec criteria 1–3 (static facts: files exist).
- [x] **V2**: `grep -q '^\.claude/\.brainstorm-state/' .gitignore` returns 0 — covers spec criterion 4 (gitignore entry present).
- [x] **V3** (synthetic via Python renderer + Playwright): Manual run of Task 12 (start → contribute → checkpoint → lens → done → browser) succeeds end-to-end — covers spec scenarios *start a free-form session*, *free-form rounds capture material*, *checkpoint after divergence*, *lens applied on demand*, *render to HTML and report the local-serve command*, *HTML output is self-contained and openable offline*.
- [x] **V4** (mechanical-only — see Task 13 note): Manual run of Task 13 (resume after close) succeeds — covers spec scenario *resume an existing session*.
- [x] **V5** (heuristic defined in SKILL.md; behavioural check deferred to fresh CC session): With ≥10 ideas all tagged `easy`, the next checkpoint includes a named lens suggestion ("all ideas are `easy` — Reverse would force…") — covers spec scenario *skill detects maturation and suggests a lens*.
- [x] **V6** (definition in SKILL.md; behavioural check deferred): `/brainstorm list` after Tasks 12-13 prints both sessions with topic / timestamp / counts — covers spec scenario *`/brainstorm list` enumerates past sessions*.
- [x] **V7** (Playwright button-click confirmed clipboard write + markdown content inspected): In a browser (served via `python3 -m http.server`), clicking `Copy as markdown for /sdd new` writes a markdown block to clipboard containing the topic, ideas-by-tag, open questions, and lens summaries — covers spec scenario *HTML offers a one-click markdown export for spec promotion*.

## Notes

- **Plan drift to track**: anything that comes up during Task 12 visual checks (CSS quirks, mermaid version mismatches, clipboard behaviour by browser) goes in `plan.md` § *Risks and unknowns* as a resolved-or-deferred item, not silently absorbed.
- **CDN pinning**: Task 3 pins `markmap-autoloader` and `mermaid` to specific versions. If the chosen versions are EOL or change rendering quirkily during smoke test, update both pins in `plan.md` § *Research / citations* before re-running Task 12.
- **Composability deferral (Open Q2)**: if real usage in Task 12 surfaces a strong need to chain lenses in one prompt (e.g. "SCAMPER through Black Hat"), file as v2 follow-up — do not extend SKILL.md mid-implementation.
- **Auto-open browser (Open Q1)**: do not implement in v1. Print-the-URL pattern is sufficient. If real usage shows the URL is consistently re-typed manually, revisit with WSL2-aware platform detection.
