# Claude Code — personal config and local state (do not version)
.claude/settings.local.json
CLAUDE.local.md
.claude/projects/

# Statusline runtime state (branch cache, per-turn token snapshots, context markers)
.agent/runtime/
