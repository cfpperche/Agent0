# Reminders

- Explore CC hooks underused em Agent0 — especialmente UserPromptSubmit pra detectar prompts ambíguos/sem-contexto antes de virarem turno desperdiçado. Outros candidatos: Setup, UserPromptExpansion, Notification, PostMessage. Ver `.claude/memory/cc-platform-hooks.md` § The 29 events pra inventário completo; cruzar com `.claude/settings.json` hooks registrados pra achar os gaps. Avaliar 1-2 que fechem gap real e prototipar.  ·  due: 2026-05-13
- Continuar dogfood em `/home/goat/shrnk-mono/` — 3 capacities ainda não exercitadas: (1) **spec 011 failure path** — escrever teste deliberadamente quebrado, observar `PostToolUseFailure` capturar via `runtime-capture.sh`, probe.sh reportar `status: FAIL` com `inference_basis` correto; (2) **TDD advisory negative path** — sub-agent edita prod sem test no mesmo diff → `tdd-advisory:` aparece no stderr do validator (não bloqueia); (3) **lint-validator state b→a** — declarar `@biomejs/biome` em devDeps de apps/api, observar advisory `lint-advisory: biome declared but not installed`, depois `bun install` (com `# OVERRIDE` marker) → state-a (lint roda no pipeline). Sessão fresh recomendada por causa de contexto.
