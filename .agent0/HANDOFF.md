# Session handoff

Canonical runtime-neutral handoff for Agent0 sessions. Claude Code injects/nags through hooks; Codex receives the same handoff through tracked `.codex/hooks.json` after project/hook trust.

See `.agent0/context/rules/session-handoff.md` for the protocol, 4 KB size discipline, fallback behavior, and reader-side truncation defense.

---

## Current State

**Session 2026-06-05 — spec 152 `browser-primitive-consolidation` SHIPPED + COMMITTED.** (Earlier handoff text said "UNCOMMITTED" — that was stale; corrected 2026-06-05 after a `git log`/`git status` check during the spec-153 scoping meeting: commits `f31e6b8 feat(152)` + `ef6b815 chore(handoff)` landed it, spec `Status: shipped`, working tree clean. Credential-class state files remain gitignored.) First demand-validated item after the harness arc closed — driven by a real founder ask (use `agent-browser`'s full eyes+hands), graduated through a `/meeting` (Claude↔Codex↔founder, transcript `.agent0/meetings/agent-browser-visual-inspection-2026-06-05T14-38-11Z/`, `synthesis: accepted`) → `/sdd refine` → full SDD build under `/goal`. `agent-browser` (vercel-labs native-Rust CLI) is now Agent0's **primary, runtime-neutral browser primitive** (eyes+hands+observe, no per-runtime MCP wiring), wrapped by **`.agent0/tools/agent-browser.sh`** (caps · route · run · verify-contract · reset · audit-tail). Playwright + Chrome DevTools MCP = **permanent fallback** (founder call — never deleted; also the no-binary degradation path). Validated for real: installed agent-browser 0.27.1 + system Chrome, **suite 44/44 incl. 2 live dogfood slices** (`bash .agent0/tests/agent-browser/run-all.sh`): visual-contract `verify-contract` PASS + negative gate; auth-gated `state` save/load reuse with a negative control. `doctor.sh` → `[ ok ] agent-browser`. harness-sync 42/42 + agent0-status 34/0 still green (touched sync-harness.sh manifest + doctor.sh). Spec `Status: shipped`, all AC ✓, notes.md filled.

**Then — spec 152.1 `audit` primitive, demand-validated by a REAL dogfood (UNCOMMITTED).** Founder asked for real dogfood → audited the actual Agent0 consultancy site (`site/dist/`, 21 pages pt/en/es) with the primitive. **Site clean** (1 h1, main+nav, 0 console errors everywhere). The payload was 4 primitive-gaps from real use — chiefly: hand-rolled a11y parsing is a trap (`grep -c 'level=1'` ALSO matches `listitem [level=1]` nesting → mis-flagged clean home pages as `h1=8`; actually 1+7 listitems), and there's no multi-page sweep. Founder chose "build now" → added `audit <base> --paths …` (sweep → report.{md,json}+shots; gate = one h1 + main + console≤max; vitals advisory) + `parse-structure` (heading-lines only, never miscounts). **Loop closed:** re-audited the real site via the new primitive → every page correctly `h1=1`, 0 false positives. Suite now **9 files / 58 asserts, all pass** (added `06-structure`, `12-dogfood-audit`, `bad-page.html` fixture). Recorded as 152.1 in `docs/specs/152.../notes.md`.

**Then — spec 152.2 `browser-login` + `adopt` (human-in-the-loop auth flow), UNCOMMITTED.** Founder wanted to dogfood the auth-gated path (GitHub/X/LinkedIn). Hard env finding: an **agent-spawned headed Chrome won't stay visible** (WSLg drops the window + the harness reaps the agent's process tree; the Chrome *process* survives but the *window* dies) — so the human must own the browser and the agent attaches over CDP. Founder: "make it a flow, I'll forget the command." Built `.agent0/tools/browser-login.sh <host>` (human runs ONE word → detached, isolated, dedicated-profile Chrome with CDP `:9222` at the login page) + `agent-browser.sh adopt <host>` (agent polls the CDP `/json` HTTP endpoint — non-disruptive — until the host page leaves the login flow, then saves state; `--detect-only` = "logged in yet?"). Validated end-to-end synthetically (browser-login → CDP-simulated login → adopt auto-detect+save → adopted state reuses authed). Suite now **11 files / 68 asserts, all pass** (added `07-adopt-detect`, `13-dogfood-adopt`, `fake-cdp.js`; `run-all.sh` tears down the daemon between live scenarios to kill contention flakes). **Then — REAL auth dogfood RUN: GitHub + X + LinkedIn, all 3 PASS end-to-end.** Founder logged into each (agent never touched credentials); `adopt` auto-detected + saved state; headless reuse reached authed content on all three (GitHub 14 cookies → `settings` showing `cfpperche`; X 14 → `home` composer; LinkedIn 45 → `feed` nav, no authwall). The meeting's riskiest axis (real httpOnly auth migration) is validated in production conditions. Finding: the **agent-launched detached Chrome window stayed visible** for all 3 — the earlier window-death was specific to agent-browser's *headed daemon spawn*, not plain detached `google-chrome`. **Credential-class state files now exist** (gitignored): `.agent0/.runtime-state/agent-browser/state/{github,x,linkedin}.json` — they hold the founder's real session cookies. **Founder chose to KEEP all 3 (2026-06-05)** for reuse — gitignored, credential-class; any local process with disk access can use them while they exist.

_Prior — 149/149.1/150.x + 151 shipped + propagated (harness arc); see git log._

## Active Work

- **Spec 152 is COMMITTED + shipped** (`f31e6b8` + `ef6b815`); working tree clean. No staging work pending.
- **Spec 153 (NEXT) — `decouple-harness-from-playwright` — SCOPED via `/meeting`, not yet built.** Founder intent: make the Agent0 harness depend on `agent-browser` **exclusively** for browser-driven functionality; Playwright/Chrome-DevTools MCP survive ONLY as opt-in `.mcp.json.example`/`.codex/config.toml.example` templates — never a fallback the routing or a first-party skill degrades to. Meeting `.agent0/meetings/descacoplar-harness-agent0-do-playwright-2026-06-05T17-44-39Z/`, `synthesis: accepted`. Codex independently validated the scope and **hardened it: the coupling is in CODE, not just rule prose.** Decouple set — (1) **`/product` visual gate** (`SKILL.md:58` seeds a live `<out>/.mcp.json`; `:151/161/164`, `quality-checklist.md:133`, `sdd-handoff.md:69`, `state-machine.md:113`, `serve-hifi.sh`, `report.md.tmpl:77` drive `mcp__playwright__*`) → repoint to `agent-browser.sh verify-contract`/`audit`; (2) **`browser-auth.md`** still Playwright-led → convert to `browser-login.sh`+`adopt` (replacement already exists/validated); (3) **`agent-browser.sh` routing CODE** (`:147-162` fallback lanes, `:213-216`, `:257-258`, `:401`) + the tests that lock it (`tests/agent-browser/02-route.sh`, `04-audit.sh`); (4) **`runtime-capabilities.md:56-57`** + **`doctor.sh:144,152`** wording; (5) cleanup **`secrets-scan.md:97`** ("Playwright-produced" framing). **Policy decided:** explicit browser commands (`run`/`verify-contract`/`audit`/`adopt`) **fail-closed** (rc 4, install/doctor/caps message, NO MCP) — the `/product` Phase-4 gate stays **skip-advisory** (best-effort, never blocks) but must NOT seed/mention MCP and the skip must be LOUD in REPORT. Templates + harness-sync untouched-tests STAY (correctly bucketed). Next: graduate to `/sdd refine` → spec 153, build on clean `main`.

## Next Actions

Two founder-named follow-ups to spec 152 (both `/meeting`-first — each shifts a load-bearing decision, so deliberate before building):

1. ✅ **DONE (scoped) — `/meeting` to retire Playwright MCP as a harness dependency.** Held 2026-06-05, `synthesis: accepted` (see Active Work § spec 153 for the validated decouple-set + the fail-closed-vs-skip-advisory policy decision). Immediate next: `/sdd refine` → spec 153, build on clean `main`.
2. **`/meeting`: incorporate visual contracts into SDD + the delegation gate, where pertinent.** When a spec/task produces UI, SDD should treat a `verify-contract`/`audit` pass as an acceptance artifact, and the delegation gate (`delegation.md` 5-field DONE_WHEN/DELIVERABLE) should require visual-contract verification for UI-producing sub-agent tasks. "Where pertinent" is the hard part — scope the trigger (which specs/tasks count as UI-producing) so it gates real UI work without over-gating non-UI work.

- `/squad` worktree-per-agent (v2) remains noted in `rules/squad.md` but unbuilt — no demand yet.

## Decisions & Gotchas

- **agent-browser daemon/port gotchas (verified, cost real debug — in `.agent0/memory/agent-browser-primitive.md`):** `close --all` HANGS with no daemon (wrapper `reset` guards it); the global daemon ignores `--profile`/`--state` if already up (use `reset` or isolated `--session <name>`); kill it by argv0 match (`agent-browser-linux*`), NEVER `pkill -f agent-browser` (matches the calling shell → self-kill, exit 144); `wait <ms>` is an unreliable settle (use `wait <selector>` or shell `sleep`). JSON envelope `{success,data,error}`; pinned 0.27.1.
- **Routing (anti-ambiguity, load-bearing):** agent-browser default; MCP fallback only on `{no-binary, no-chrome (AGENT0_BROWSER_NO_CHROME=1), override (AGENT0_BROWSER=mcp)}`. Missing *system* Chrome is NOT a fallback reason — agent-browser self-provides Chrome-for-Testing.
- **Deliberate deviations (notes.md):** policy is JSON not YAML (`jq` is a hard dep); availability surface is `doctor` not the SessionStart brief (no per-boot binary probe). Founder chose permanent MCP fallback over hard cutover → the meeting's minority report (fallback-deletion pace) is moot (no deletion).
- New tracked file `.agent0/browser-policy.json.example` registered in `sync-harness.sh` COPY_CHECK_FILES; `CLAUDE.md` + `AGENTS.md` managed-block index kept in parity (both got the `## Browser primitive` section).
