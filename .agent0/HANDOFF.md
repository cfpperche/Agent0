# Session handoff

Canonical runtime-neutral handoff for Agent0 sessions. Claude Code injects/nags through hooks; Codex receives the same handoff through tracked `.codex/hooks.json` after project/hook trust.

See `.agent0/context/rules/session-handoff.md` for the protocol, 4 KB size discipline, fallback behavior, and reader-side truncation defense.

---

## Current State

**▸ Session 2026-06-07 (latest) — Spec 164 `paid-media-kit` SHIPPED, COMMITTED + PUSHED `ed47b6e` (`5e20de6..ed47b6e`).** The deferred second layer of 163. Full pipeline founder-directed in one loop ("you and codex do the sdd and implementation; don't let followups resolve everything"): decision-grade `/meeting` (claude+codex, spec-149 blind openings **converged independently** — both opened from sealed files on the same shape; ledger 11 claims / 0 assertion-only, all anchors verified; minority report; accepted) → kill-gate measurement (Explore-mapped concerns A–E) → `/sdd new`+spec/plan/tasks → implement → Codex adversarial diff review (verdict FIX-FIRST → 2 findings, both fixed+proven). **The honest re-scope is the headline:** the handoff's optimistic 5-target plan collapsed under measurement to **4 PURE helpers** in `.agent0/tools/lib/paid-media.sh` — `pm_yaml_top`/`pm_yaml_tier_field` (the `*-tiers.yaml` block-scan oracle, = sound's old `yget`/`ytop` verbatim) + `pm_has_fal_key`/`pm_fal_key_state` (leak-safe FAL_KEY). **Separate lib, not folded** (cohesion: capacity.sh is local/free; paid is a different cluster). **Migrated `sound` + `audio --remote`** (the 2 tools-dir paid tools that source the kernel); sound is byte-identical by construction (1-line `$TIERS` binders over the moved readers), audio's inline-awk→shared-reader **proven byte-identical empirically** for `audio-tiers.yaml` before any code. **Stayed LOCAL (genuine variants, NOT extracted):** cost formula, the `--confirm-cost-usd` gate (conflicting policy: sound hybrid-threshold vs video hard vs none), fal invocation (sync/async); a `pm_require_fal_key` + `pm_fal_run`/`pm_fal_download` were **rejected** (can't carry the divergent `|| cap_fail`/`|| fail` tails / pure-contract). **`image` OUT** (pipe-table not YAML). **`video` = the named reopen-trigger** (its YAML reader could move, but that makes a skill-dir tool source `tools/lib/` — a NEW cross-dir portability/sync concern needing its own source/sync smoke; NOT a deferred slice of this pass). **Codex's 2 catches (both fixed):** `golden.sh` was non-hermetic re ambient FAL_KEY → now pins `env -u FAL_KEY` (the set state is pinned by the new `paid-golden.sh`); `paid-golden.sh` missing-tool branch now sets `fail=1`. **New gate `paid-golden.sh`** pins sound/audio caps/doctor under FAL_KEY **set AND unset** + a key-value leak guard. **Gate GREEN (zero behavior change, 11/11):** golden verify (hermetic, proven under both ambient FAL_KEY states) + paid-golden + sound suite + audio suite + sync-propagation (already covered paid-media.sh) + missing-kit-guard (capacity + paid-media both → exit 70) + bash -n (3 scripts) + doctor (22 ok/0/0) + 0 FAL_KEY leaks. All 4 spec OQs resolved in the meeting. Spec `docs/specs/164-paid-media-kit/`. Meeting `.agent0/meetings/paid-media-kit-honest-scope-2026-06-07T01-33-04Z/`. **Memory:** `capacity-spec-index.md` updated with the 159–164 media family. **Gotcha (163 redux):** source the paid lib BELOW each tool's `--help` `sed` range (sound 3-22, audio 3-30) — done. **Consumer-facing → founder-triggered harness-sync, not yet run.**

**▸ Session 2026-06-06/07 (latest) — Spec 163 `capacity-kit` SHIPPED, COMMITTED `cd97e72` (NOT pushed).** The meta-move after the media family: extract the shared capacity-tool plumbing into a sourced kernel `.agent0/tools/lib/capacity.sh` so the 7th tool is config, not a clone. Full pipeline: decision-grade `/meeting` (spec-149, blind openings converged independently; ledger 6 claims / 0 assertion-only; minority report; accepted) → `/sdd refine→plan→tasks` → **kill-gate measurement** (have/emit_exit/sha256_str byte-identical ×3-4 → lib, not template) → `/squad init`+turn-1 (golden+kernel+diagram byte-identical) → **founder chose solo completion (option c)** → finished + gated green. **Kernel:** `cap_have`/`cap_sha256_*`/`cap_emit_exit`/`cap_manifest_append`/`cap_fail` (+`_cap_on_fail` manifest hook)/`cap_resolve_ffmpeg`; small helper lib, not a framework. **Migrated** audio/sound/transcribe/diagram. **Honest scoping:** diagram/sound use `cap_fail` (compact JSON); transcribe (rich) + audio (pretty) keep a LOCAL `fail` (changing their `--json` shape = behavior change). **Load-bearing sync fix:** added `.agent0/tools/lib|*.sh` glob (was maxdepth-1, missed lib/; retired managed-block literal) — else consumers source a lib that never shipped. **DEFERRED follow-up:** paid-media sub-kit (`lib/paid-media.sh` — tier-reader/cost-gate/fal-wrapper; ~2 tools today) + image/video migration. **Gate GREEN (zero behavior change):** 5 suites + `golden.sh verify` (caps/doctor/--help/usage/badflag byte-identical before-vs-after) + sync-propagation + missing-kit-guard + bash -n + doctor (22 ok/0/0) + validator + full harness-sync suite. All 4 OQs resolved. Squad run aborted cleanly (turn-1 work kept). **Gotcha:** `--help` via `sed -n A,Bp "$0"` drifts if the kernel `source` is inserted inside that range — source below it. Spec `docs/specs/163-capacity-kit/`. Meeting `…/capacity-tool-kit-consolidation-2026-06-07T00-28-12Z/`.

---

**▸ Session 2026-06-06 — Spec 162 `/diagram` SHIPPED + dogfood-proven, COMMITTED `5db7cb3`, PUSHED.** Full pipeline in one go per founder direction: strategic discussion → decision-grade `/meeting` (claude+codex, spec-149 blind openings converged independently; ledger 7 claims / 0 assertion-only; minority report preserved; `synthesis: accepted`) → `/sdd refine`(seeded by meeting)→plan→tasks → build → dogfood. **`/diagram` = deterministic technical-visual generation** (Mermaid → SVG/PNG/PDF), the `/transcribe`-class LOCAL/FREE member of the structured-capacity family — deterministic sibling of `/video --mode code`, counterpart to `/image` (paid). **Strategic rationale (founder):** demand is via consumer products — esp. **cognixse, which serves software-dev companies that ship diagrams as product artifacts** (a vertical lever, not internal tooling). Render: `npx -p @mermaid-js/mermaid-cli mmdc` reusing **system Chrome** (puppeteer cfg + `PUPPETEER_SKIP_DOWNLOAD=1`); **validation-only degradation** when Chrome absent (structural check + source kept, never dead). Mermaid-only v1 (d2 = reopen-trigger). Diagrams are keepers (`.mmd`+SVG git-tracked, `assets/diagrams/` default; no draft class). **Validated:** offline 39 assertions/5 scenarios PASS; `/skill validate` rc0; doctor 22 ok/0/0. **Dogfood (real local renders):** architecture+sequence+ERD SVGs committed under `assets/diagrams/`, error path proven live. All 4 OQs resolved. **Minority-report consumer-ship gate SATISFIED by the dogfood** — but harness-sync stays founder-triggered. Spec `docs/specs/162-diagram/`. Meeting `.agent0/meetings/diagram-capacity-technical-visuals-2026-06-06T22-34-59Z/`.

---

**▸ Session 2026-06-06 — Spec 161 `/sound` SHIPPED + dogfood-proven, COMMITTED `1c7e933` (NOT pushed).** Autonomous build per founder directive (OPTION 2: tasks→impl→validate→dogfood). Paid-only creative audio (music + SFX) — the `/image brand` analog, completing the audio media family (transcribe STT + audio TTS + sound creative). Headline data-driven `sound-tiers.yaml` oracle (per-model body+url shapes) **proven LIVE** by two real cheap fal calls (SFX `.audio.url` + CassetteAI music `.audio_file.url`, ~$0.0093). 58 assertions/6 scenarios PASS; `/skill validate` rc0; doctor 21 ok/0/0. Full detail in Next Action 0.7 + `docs/specs/161-sound/notes.md`. **Founder triggers push + harness-sync.**

---

**▸ Session 2026-06-06 — `attempt-before-handoff` browser rule SHIPPED, UNCOMMITTED (founder reviews diff).** Founder friction: consumers' agents keep **devolvendo trabalho de browser pro humano por reflexo** ("abra essa URL e confira / não dá pra automatizar daqui") instead of driving `agent-browser`. Investigated the live cognixse session (`~/.claude/projects/-home-goat-cognixse/8a6605ab-…jsonl`, spec 020 anti-flood): the agent **had agent-browser + knew it, punted anyway with zero evidence, only tried after the human pushed back** → proves it's a **behavioral-default bug, NOT discoverability** → a `/browser` skill would NOT have fixed it (the founder's first hypothesis, correctly rejected). Ran a **decision-grade `/meeting` claude+codex** (`.agent0/meetings/browser-attempt-before-handoff-2026-06-06T17-40-20Z/`): blind openings converged **independently** on every call; ledger **7 claims, 0 assertion-only**, `synthesis: accepted`. Founder chose **apply-direct** (not SDD). **Shipped (3 files, +22/−2, UNCOMMITTED):** new **§ Attempt-before-handoff** in `browser-primitive.md` (try via agent-browser or prove an *observed* blocker before any human punt; bounded "really tried" criterion route/caps→attempt-to-1st-blocker→`BROWSER_LOGIN_REQUIRED` for auth→observe Turnstile/CAPTCHA not infer; anti-overcorrection: NOT "never delegate", the cognixse Turnstile punt was legit *as a conclusion*, wrong *as a first move*; **rule-only, no hook** — punt is text not a tool-call, `user-prompt-framing.md` precedent) + 1 mirror sentence in `CLAUDE.md`+`AGENTS.md` managed blocks. **Reopen-trigger armed (rule-of-three):** ≥3 new speculative punts post-rule → reconsider a `Stop`/output-lint hook (logged `unresolved` in ledger on purpose). No memory entry (fix lives in the synced rule, not project-local memory). **NOT committed/pushed; consumers get it on next harness-sync — founder decides whether to sync cognixse/mei-saas now.**

---

**▸ Session 2026-06-05 — Spec 158 `frontend-designer` skill SHIPPED (committed, NOT pushed).** Founder `/goal`: discuss-with-Codex → SDD → implement+validate → dogfood 3 `/tmp` demos. **Decision-grade `/meeting`** (claude+codex, blind openings + pressure-test, ledger 5 claims all path-anchored, `synthesis: accepted`) → graduated to **spec 158** → built the skill → **dogfooded 3 demos, all proven.** The skill is the build-time **craft loop** filling the gap between `/product` (planning, no runnable app) and the spec-155 visual-contract gate (acceptance): `create`/`refine`/`explore` modes, project-derived stack ladder (no frozen defaults), mandatory artifacted `reference-research.md`+`design-direction.md` pair, done-proof reuses `agent-browser verify-contract` (unavailable=blocker, never pass), native-only surfaces use honest labeled evidence. Canonical `.agent0/skills/frontend-designer/` (+symlinks), rule `frontend-designer.md`, CLAUDE.md block. `/skill validate` exit 0; shell test 20/20; doctor 18 ok. Dogfood (`/tmp/FD-DOGFOOD-SUMMARY.md`): A create+reused-DS web → verify-contract 6/6 PASS; B refine web → before FAIL 3/after PASS 7/7 (loop caught a real a11y bug iter 1→2); C native Expo/RN no-DS → node --test 7/7, no visual-contract claimed. Commits `…158…` (meeting, spec, impl, closeout). **One open question:** does `explore` mode earn its place (re-evaluate after first real use). **NOT pushed — human triggers push.**

---

**▸ Session arc 2026-06-05 (earlier — all on `origin/main`):** shipped **155** visual-contract-acceptance-gate (founder-triggered the lead deferred candidate; built via decision-grade `/meeting` → `/sdd` → `/squad`, 2 real defects caught by symmetric cross-review) → shipped **156** sync-exclude-od-regenerated-state (the `COPY_CHECK_SEED` ship-once/never-reconcile mechanism; motivated by a live cognixse sync) → synced the harness to **cognixse** (`74b52fc`) + **mei-saas** (`9dedd0c`), both committed/pushed, both clean (spec-156 seed killed the OD `!! customized` drift) → explored telemetry-for-the-hard-gate as **157**, ran a decision-grade `/sdd debate` with Codex, and **killed 157** (`b04501d`) as over-built/premature, recording a founder reopen-trigger for the hard gate. Details below.

---

**Spec 156 `sync-exclude-od-regenerated-state` SHIPPED + MERGED + PUSHED** (`a89048d`, `Status: shipped`). Solo SDD build (plan→tasks→impl→validate, no `/squad`). Adds a **seed-file** disposition to `sync-harness.sh` (`COPY_CHECK_SEED`): the three `/product` OD-engine-regenerated files (`od-catalog-index.json` — read at `/product` runtime; `vendor/open-design/.cache/ds-index.json`; `vendor/open-design/MANIFEST.json`) are now **ship-once, never-reconcile** — copied only when absent (cold-start preserved), left untouched once present (never compared/flagged/overwritten-under-force/deleted), and not recorded in the manifest, so a drifted consumer **self-heals in one `--apply`** (new baseline omits them). Distinct from spec-144 `COPY_CHECK_EXCLUDE` (drops entirely → would break cold-start). Test `43-seed-od-state.sh` (12 assertions), full harness-sync suite green, validated live on `cognixse` (the three now `= seed`, not `!! customized`). All 4 spec OQs resolved. Motivated by the 2026-06-05 cognixse sync drift (this session). **`cognixse` self-heals its 3 stale baseline entries on its next `--apply` — no action needed.**

_Prior this session — Spec 155 `visual-contract-acceptance-gate` SHIPPED + MERGED + PUSHED to `origin/main`_ (`356d441` plan + `03a9a54` impl; `Status: shipped`; `fb0da55..03a9a54` pushed). The spec-152 follow-up #2 (was the lead DEFERRED candidate) — built end-to-end through the full pipeline: decision-grade `/meeting` (blind commit/reveal, anchored ledger, minority report — claude+codex converged independently) → `/sdd` (spec/plan/tasks/notes, **all 6 OQs resolved, 0 deferred follow-ups**) → fail-closed `squad.json` → **`/squad`** autonomous build (6 rounds, 3 claude / 3 codex, `repair_attempts 1/4`, `ready_for_human_prod`). **What shipped (28 files, +1445/−25):** `UI impact: none|render|interaction|flow` declaration (in SDD spec template, default none); `.agent0/tools/ui-impact-detect.sh` (diff heuristic, declaration-first, suggests-never-sets); validator emits non-blocking `visual-contract-advisory:` on undeclared-UI-surface-change AND declared-UI-missing-evidence; `agent-browser.sh verify-contract` **extended past render to `interactions`+`flow` tiers** (drives act-verbs + asserts post-state; `flaky`→non-fatal warn; unavailable≠pass; backward compatible); delegation reuses the 5 fields (no 6th), `delegation-verify` checks the evidence bundle; canonical rule `visual-contract.md` + CLAUDE.md/AGENTS.md index + `/product` reconcile; offline suite `.agent0/tests/visual-contract/` (fake-bin + stateful per-route flow test). **Symmetric F2 cross-review caught + fixed 2 real defects:** verify-contract closed the browser before interaction/flow steps; validator didn't warn on declared-UI-missing-evidence. Gate 12/12; both test suites green; advisory→hard hardening explicitly OUT of 155 (future dogfood-gated spec). Meeting transcript: `.agent0/meetings/visual-contracts-sdd-delegation-gate-2026-06-05T21-29-24Z/`.

_Prior this session — TWO parallel workstreams reconciled onto `main` (now pushed):_

**(a) Spec 154 `squad-hardening` SHIPPED + MERGED** (`2392590`, `Status: shipped`) — three real defects the spec-153 `/squad` run surfaced, each recurring on every squad run: (1) the `squad.json.example` `forbidden_paths` `"secrets"` was unanchored → false-matched `secrets-scan.md` → now `(^|/)secrets?/` + `\.secrets?(\.[^/]+)?$`; (2) the session-handoff Stop-nag (`session-stop.sh`, both runtimes) fired on every `codex/claude exec` peer turn → blocked the peer into rewriting the orchestrator-owned `HANDOFF.md` → fixed at the bridge (both `codex-exec.sh`+`claude-exec.sh` export `CLAUDE_SKIP_SESSION_HOOKS=1`); (3) no non-destructive abort recovery (`rollback` discards work) → added `squad.sh resume` (re-baselines + keeps work; refuses a genuine forbidden touch without `--force`). Tests `12`/`13`/`14`, full squad suite **14/14**. Docs: `squad-contract.md` + `rules/squad.md`.

**(b) Site "Em construção" / maintenance mode COMMITTED** (`28a1e4b`, a parallel session). Founder ask: "coloque o site do Agent0 em modo Em construção — não excluir nada." Non-destructive — every page stays in source, wrapped in an `if`. **5 site files:** new `site/src/config.ts` (toggle `UNDER_CONSTRUCTION`, default `true`, env override `PUBLIC_UNDER_CONSTRUCTION=false`); new `site/src/components/Maintenance.astro` (trilingual EN/PT/ES, brand tokens, `noindex`, repo link); gated `Landing.astro` + `Doc.astro` + `pages/index.astro`. Validated `bun run build`: all 22 pages serve maintenance per-locale, zero old-content leak, `noindex` present; off-path restores the full site. **NOT pushed** → site not live yet (deploy fires only on push to `main` touching `site/**` via `.github/workflows/deploy-pages.yml`). To re-enable later: flip `true`→`false` in `site/src/config.ts`.

The two were kept as separate, non-mixed commits (scoped staging). **`main` is 6 commits ahead of `origin/main`, unpushed** — the human triggers the push (which will deploy the maintenance site).

_Prior this session — spec 153 `decouple-harness-from-playwright` SHIPPED + MERGED to `main`: the harness now depends on `agent-browser` **exclusively** — Playwright/Chrome-DevTools MCP survive ONLY as opt-in `.mcp.json.example`/`.codex/config.toml.example` templates, never a fallback the routing or any first-party skill degrades to. Flow: `/meeting` (Codex validated + hardened the scope — "coupling is in CODE, not just prose", `synthesis: accepted`) → `/sdd refine` (all 4 OQs resolved at plan time, 0 follow-ups) → **`/squad`** autonomous build (Claude↔Codex ping-pong, 4 bands), `ready_for_human_prod` at round 8, **repair_attempts 0**.
  - **What changed (27 files):** `agent-browser.sh` `route` is fail-closed (`primary`|`unavailable:{no-binary,no-chrome,mcp-removed}`; explicit cmds `run`/`verify-contract`/`audit`/`adopt` → rc 4, or rc 3 for the removed `AGENT0_BROWSER=mcp` override — **no MCP lane**); `audit` gained `--structure strict|optional` (strict = unchanged 152.1 gate; optional = h1/main advisory for fragments) + responsive 375/1280 screenshots & `scrollWidth>clientWidth` overflow fields (via a fixed internal `eval`). `/product` Phase 4 now sweeps hi-fi via `agent-browser.sh audit --structure optional` over `file://` (no `.mcp.json` seed, no HTTP server, loud skip when unavailable). `browser-auth.md` rewritten agent-browser-native (`browser-login.sh`→`adopt`; signal renamed `BROWSER_AUTH_REQUIRED`→**`BROWSER_LOGIN_REQUIRED`**; state at `.agent0/.runtime-state/agent-browser/state/<host>.json`). `serve-hifi.sh` deleted. `.browser-state/` scaffold retired (manifest/README/gitignore). Converted: `browser-primitive.md`, `runtime-capabilities.md`, `secrets-scan.md`, `doctor.sh`, `context-inject.sh`, `CLAUDE.md`+`AGENTS.md` (managed blocks, parity), `site/i18n/capacities.ts`. New `08-no-mcp-coupling.sh` grep-guard + `02`/`04` rewritten + `12` extended.
  - **Validated:** agent-browser suite **GREEN incl. live dogfood** (`run-all.sh`), harness-sync **42 PASS** (templates byte-stable — `11`/`35` green), validator + doctor (rc 0, no MCP remedy). Squad gate green; both agents propose-done. **Codex's adversarial review caught a real gap** — `adopt` was missing the fail-closed guard (AC#1 names it) → repaired + tested (`04-audit` 16/0). The 151-F2 peer-review value, realized.
  - **Predecessors 152/152.1/152.2 shipped + committed** (`f31e6b8`+`ef6b815`). Live new-mechanism auth state (`{github,x,linkedin}.json`) exists, gitignored.

_Prior — 149/149.1/150.x + 151 + 152.x shipped (harness arc + browser primitive); see git log._

## Active Work

- **Consumer harness-sync 2026-06-07 — `sound`(161)/`diagram`(162)/`capacity-kit`(163)/`paid-media-kit`(164) propagated to both consumers, COMMITTED + PUSHED.** `cognixse` `267aa21` (already on remote at push time — the local "ahead 3" was a stale tracking ref; fetch resolved HEAD==origin/main), `mei-saas` `3a394c6` (`7efe9b6..3a394c6`). Each: **60 copied, 0 customized-refused, 0 overwritten** (cognixse 7 stale-updated, mei-saas 6). **Load-bearing fix verified live:** both consumers had `lib/` with only `managed-block.sh` (NO `capacity.sh`) — their kernelized `audio`/`transcribe` would have sourced a missing lib. The sync carried `lib/capacity.sh` + `lib/paid-media.sh` + the kernelized tools together; post-sync smoke `audio/sound/diagram/transcribe caps` all exit 0 (no orphaned source) on both. Delta is harness-only (no product code; `assets/{diagrams,sound}/.gitkeep` scaffolding only).

- **`attempt-before-handoff` rule COMMITTED + SYNCED, NOT pushed (this session).** Agent0 `c4750a6` (rule + managed blocks + meeting record + handoff). Harness-synced to **cognixse** (`0549143`) + **mei-saas** (`8485202`) — `--apply` RC=0, exactly the 3 expected stale files (browser-primitive.md, AGENTS.md, CLAUDE.md managed) + baseline regen, **0 customized-refused**. **All 3 repos local-only — founder triggers the pushes.** (Untouched untracked siblings left out of every commit: Agent0 `docs/specs/159-transcribe/`, cognixse `docs/specs/021-email-notifications/`, mei-saas `assets/brand/tino-wordmark.png`.)
- **Nothing else in flight.** Spec 158 `frontend-designer` SHIPPED + **PUSHED** (Agent0 HEAD `231de67`; working tree clean, 0 unpushed). **6 dogfoods** (`/tmp/fd-demo-{a..f}`, summary `/tmp/FD-DOGFOOD-SUMMARY.md`): A create+reused-DS web (6/6), B refine web (FAIL3→PASS7), C native-honesty (node 7/7), D Patudo brand landing (6/6), E explore 3-directions (zero code), F animated Three.js hero (motion proof boundary). Open questions all resolved (`explore` KEEP-narrowed).
- **`/image` → `frontend-designer` integration SHIPPED (2026-06-06).** Decision-grade `/meeting` (`.agent0/meetings/frontend-designer-image-integration-*`, Claude+Codex, ledger 6 anchored) + dogfood-proven (Patudo hero, `/image --tier=draft` beat emoji, contract still 6/6). Implemented as **opt-in guidance, no machinery**: `references/imagery.md` + craft-loop step + SKILL.md/rule. Limits: create/refine only (never explore); draft-tier auto (brand=/product); **tracked-neutral default**, never import a gitignored draft; graceful degradation (key-absent/no-credits/402/network → `image-fallback:<reason>`, build+contract pass); verify neutral path with draft disabled; redact stderr; one attempt/surface; never hard dep.
- **Consumers in sync + pushed (2026-06-06):** `cognixse` (`dd44120`), `mei-saas` (`7efe9b6`) — both now carry the **audio family `/transcribe`+`/audio`** (specs 159+160) on top of prior syncs (158 + explore sharpening + animated/WebGL done-proof + `/image` integration + browser attempt-before-handoff). `--apply` clean: **27 copied, 0 customized-refused, 0 overwritten** each. **Caught+fixed a sync gap mid-run:** `audio-kokoro.py` (the Kokoro shim) was outside the `*.sh` manifest glob → added to `COPY_CHECK_FILES` + the two audio-dir `.gitkeep`s (Agent0 `0eae811`) so the shim actually ships (else Kokoro breaks on consumers).

## Next Actions

0. ✅ **DONE — `capacity-kit` PAID-MEDIA SUB-KIT shipped as spec 164** (this session, 2026-06-07). `.agent0/tools/lib/paid-media.sh` (4 pure helpers) + migrated `sound`+`audio --remote`, gated green (11/11, zero behavior change). The handoff's optimistic 5-target scope was honestly cut by the kill-gate measurement to readers+FAL_KEY-state only (cost-gate/formula/async are genuine variants → stayed local; image out; video = named reopen-trigger). See Current State (latest) + `docs/specs/164-paid-media-kit/`. **Committed + PUSHED `ed47b6e`.** One carry-over: the **`video` YAML-reader reopen-trigger** — only when a skill-dir→`tools/lib/` cross-dir source/sync smoke test is in scope. Also: **consumer harness-sync** for the new `paid-media.sh` is founder-triggered, not yet run.

0.1. ⏸️ **PAUSED (founder reflecting, 2026-06-06) — integrate `/video` into `/frontend-designer`.** Mirror the `/image` integration; same discipline (decision-grade `/meeting` → dogfood-prove demand → narrow opt-in guidance, graceful, never hard dep). **Discussion already narrowed the demand (this session, no machine yet):** of three candidate uses — (A) UI motion = already covered by the craft-loop "drive and see", out of scope; (B) killer-flow walkthrough = likely `/product`/marketing, blurs the `/product`↔designer boundary; (C) **decorative `<video>` in the UI = the only 1:1 parallel of `/image`** — the honest filter **inverts the handoff's prior guess**: the demand-fitting case (C) is served by **`generative`** (organic/photoreal motion CSS can't fake), NOT `code` (HyperFrames renders HTML→MP4, which is circular for a hero that's already HTML; code-mode fits case B, the out-of-scope one). So if it ships: **case C only, generative-only**, a `motion-imagery.md` sibling of `imagery.md` (tracked neutral poster/CSS default, `<video>` decorative override, `video-fallback:<reason>`, one attempt/surface, hard `--confirm-cost-usd` inherited — never auto-fire paid video). **Live candidate to KILL with reopen-trigger** (narrow + costly; the "speculative=harness-drift" instinct is cutting). Founder reflecting before deciding ship-narrow vs kill. **`/video`-gen rule:** `.agent0/context/rules/video-gen.md`.
0.5. ✅ **Spec 159 `/transcribe` SHIPPED + DOGFOOD-PROVEN + PUSHED** (`37bc0e1`, in `df99502..37bc0e1`). Full SDD (meeting→refine→plan→tasks→impl→validate→dogfood) in one `/goal` run. **Built (dual-shape vuln-audit, runtime-neutral):** `.agent0/tools/transcribe.sh` (engine: detect→auto-acquire→ffmpeg-transcode→whisper→output→provenance-manifest; status `ok|unavailable|error` decoupled from exit code), thin `.agent0/skills/transcribe/SKILL.md` (+`.claude`/`.agents` symlinks), rule `transcribe.md` (propagation vehicle), 7 offline tests (`.agent0/tests/transcribe/`, fake whisper-cli+ffmpeg), wiring (`.gitignore`, `doctor.sh`, `CLAUDE.md`+`AGENTS.md` index). **Validated:** tests 7/7 (34 assertions); doctor 19 ok/0 adv/0 broken; `/skill validate` exit 0. **Dogfood (jfk.wav, real):** auto-acquire via `uvx --from whisper.cpp-cli` → **caught the spec's open question** — package exposes exe **`whisper-cpp`** not `whisper-cli` (default corrected; flags verified identical); `base` model fetched (147 MB, gitignored); correct transcript in ~4s; txt+srt+json; manifest `stayed_local:true`. Also fixed lang default to `-l auto` (whisper's own default `-l en` would punish non-English). All gitignore precise (transcripts/manifest/model-cache ignored; `.gitkeep` committable). **Key decisions:** local-only v1, whisper.cpp (MIT), NOT paid-media (provenance manifest, no cost apparatus); auto-acquire = ephemeral uvx only (never destructive install); diarization = named falsifiable paid-STT reopen-trigger (bar 3). `/transcribe` is consumer-facing — picked up on next harness-sync.
0.6. ✅ **Spec 160 `/audio` SHIPPED + DOGFOOD-PROVEN, UNCOMMITTED** (one founder-authorized autonomous run: refine→plan→tasks→impl→validate→dogfood). TTS synthesis, sibling of `/transcribe`. **Built:** `.agent0/tools/audio.sh` (two local engines + paid fal lane + two output classes + hybrid manifest + status/exit decoupling), `audio-kokoro.py` shim, `audio-tiers.yaml` (ElevenLabs-only tiers), SKILL.md (+symlinks), rule `audio.md`, 6 offline tests, wiring (gitignore/doctor/CLAUDE/AGENTS). **Validated:** tests 6/6 (37 assertions); `/skill validate` exit 0; doctor 20 ok/0/0. **Dogfood (real):** Piper EN synth end-to-end in 4.3s (uvx fetch + voice download + real 18KB mp3, stayed_local:true); Kokoro degrade confirmed real (espeak-ng absent here → honest unavailable+hint). **Two real bugs caught pre-commit:** (1) **FAL_KEY leak** in `doctor` (`${FAL_KEY:-unset}` printed the value) → fixed + regression test; (2) Piper HF voice URL was flat not nested → fixed. **OPEN DECISION (dogfood finding):** the zero-flag default **Kokoro fails out-of-box without espeak-ng** (a system binary uvx can't install); **Piper works with zero setup**. Founder chose Kokoro-default (quality/pt-BR) explicitly — but dogfood fired the plan's named reconsideration trigger. Decision pending: keep Kokoro-default vs make Piper the zero-flag default. **Paid lane PROVEN with a real fal call** (founder-approved): `fal-ai/elevenlabs/tts/turbo-v2.5`, real ElevenLabs mp3, cost-print + cost-fields manifest, no key leak (~$0.05 spent; test mp3 removed from tracked dir). **Founder decision: KEEP Kokoro-default** (accept the espeak-ng first-run step). Spec `docs/specs/160-audio/` (Status: shipped). **Audio family now COMPLETE** (recognition `/transcribe` 159 + synthesis `/audio` 160), both consumer-facing → next harness-sync. Committing now. Meeting: `.agent0/meetings/audio-transcribe-media-family-skills-2026-06-06T17-01-43Z/meeting.md`. Spec: `docs/specs/159-transcribe/` (Status: shipped). **Minority report still live:** local-only is hypothesized demand — a multi-speaker first dogfood would fire the diarization trigger.
0.7. ✅ **Spec 161 `/sound` SHIPPED + DOGFOOD-PROVEN, COMMITTED (NOT pushed)** — `1c7e933` (autonomous run: tasks→oracle→engine→SKILL/rule/symlinks→tests→wiring→validate→dogfood; no open question needed a Codex consult — the plan had pre-resolved all five). Paid-only creative audio (music + SFX), the `/image brand` analog, separate skill so `/audio` stays coherent. **Built:** `.agent0/tools/sound.sh` (data-driven engine), `sound-tiers.yaml` oracle (per-tier model/`prompt_field`/`duration_field`/`output_url_path`jq/`price`/`price_unit` — body+url shapes differ per model), SKILL.md (+`.claude`/`.agents` symlinks), rule `sound.md`, 6 offline tests, wiring (gitignore/doctor/CLAUDE/AGENTS/sync gitkeeps). `--kind music|sfx` REQUIRED; hybrid cost gate (print + hard `--confirm-cost-usd` >$0.25, env `AGENT0_SOUND_CONFIRM_THRESHOLD`); taste-judged (draft→`--asset`, no auto-done); `stayed_local:false` always; status/exit decoupled. **Validated:** offline 58 assertions/6 scenarios PASS; `/skill validate` rc0 (desc trimmed ≤1024); doctor 21 ok/0/0. **Dogfood (real fal, ~$0.0093 total, founder-authorized):** ElevenLabs SFX v2 3s→48KB mp3 (`.audio.url`) + CassetteAI music 10s→161KB mp3 (`.audio_file.url`) — **the per-model url-path divergence proven LIVE** (the exact bug a fixed extraction misses; the headline data-driven decision validated). **Live-unverified (gated, acceptable):** ElevenLabs Music premium ($0.80/min) endpoint/body — behind `--confirm-cost-usd`, oracle is single edit point; duration-unit caveat (`music_length_ms` gets seconds) flagged in oracle note + `notes.md`. `request_id` empty on sync lane (expected, not a bug). Spec `docs/specs/161-sound/` (Status: shipped). **Audio media family now COMPLETE: transcribe(STT) + audio(TTS) + sound(creative).** Consumer-facing → next harness-sync. **NOT pushed/synced — founder triggers.**
1. ✅ **DONE — `main` pushed** (`9164940..29bfde0`, 2026-06-05; spec 158 frontend-designer: meeting/spec/impl/closeout + dogfood notes). `explore` mode KEEP-narrowed (resolved). Skill is consumer-facing — picked up on next harness sync.
1. ✅ **DONE — `main` pushed** (`ef6b815..9840537`, 2026-06-05); GH Pages deploy fired → site live in maintenance mode. Merged branches deleted. To restore the full site later: flip `UNDER_CONSTRUCTION` `true`→`false` in `site/src/config.ts` and push.
2. ✅ **DONE — spec 155 `visual-contract-acceptance-gate` SHIPPED + PUSHED** (`fb0da55..03a9a54`, 2026-06-05). This was the lead DEFERRED candidate (visual contracts into SDD + the delegation gate); the founder triggered it and it built clean through `/meeting`→`/sdd`→`/squad` with zero open follow-ups. The "UI-producing" trigger scoping landed as the declaration-first `UI impact` field + the `ui-impact-detect.sh` heuristic.
- **DEFERRED — visual-contract HARD gate (telemetry path explored + KILLED).** 155 ships advisory-only. The "how do we get the rule-of-three evidence" question was explored as spec **157 `visual-contract-telemetry`** and **abandoned** (`3e007e9`) after a decision-grade `/sdd debate`: Claude+Codex independently judged telemetry over-built/premature (measurement for a maybe-gate, on an unreliable transcript-grep signal, single-runtime-blind; one dogfood ≠ rule-of-three). **Founder-stated reopen trigger:** *if the spec-155 advisory's pain is felt in real use (UI tasks closing without driving the UI), come back and build the HARD gate DIRECTLY — skip the telemetry.* Hard-gate design (flow-first, `SubagentStop`-blocking, `visual-contract-exempt:` override) parked in `.agent0/context/rules/visual-contract.md` § hardening trajectory; interim `jq`-on-demand recipe in `docs/specs/157-*/spec.md` § Closure; full reasoning in `157/debate.md`. **Note (2026-06-05): the cognixse dogfood already shows `verify-contract` (render+flow) run *voluntarily* against a live app — weakening the hard gate's "agents ignore it" premise.**
- `/squad` worktree-per-agent (v2) remains noted in `rules/squad.md`, unbuilt — no demand yet.
- **PARKED (founder, 2026-06-06) — Agent0 VSCode extension.** Founder floated an IDE companion; first feature = record audio → `/transcribe` → inject as prompt context, serving Agent0 + consumers. **Intent clarified: PLATFORM (voice is the wedge), not just a voice feature.** Founder dropped it "por agora." Unresolved framing if reopened: (a) it breaks Agent0's runtime-neutral / files-in-a-repo invariant — first editor-coupled + marketplace-distributed surface (not harness-sync-able); (b) the wedge is thin glue over the just-shipped `/transcribe` (good — local-first/private voice-to-agent is the real differentiator vs cloud dictation), so an extension *only* for voice is over-capitalized; (c) platform decision deserves a decision-grade `/meeting` (editor-coupling + distribution + what-beyond-audio + why-not-the-official-Claude-Code-extension); (d) cheap demand-proof first = an editor-agnostic hotkey→record→`/transcribe`→clipboard/inject script. Reopen via a `/meeting` on the platform question.

## Decisions & Gotchas

- **Visual-contract acceptance (spec 155) is a MECHANISM, advisory in v1.** Agent0 has no UI — the capability activates in consumer projects. `verify-contract` extends the existing `{required,max_console_errors}` fixture-spec with optional `interactions`/`flow` arrays (NOT a new format). `report.json` checks gained a `warn` field (`flaky` step downgrade); render-only fixtures stay backward compatible. The detector excludes `.agent0/`/docs/tests/server-langs (incl. bare `.php` — Laravel `app/*.php` false-positive; `.blade.php` matched as template first). The `.agent0/tests/visual-contract/` fake-bin stub needs `VC_FAKE_STATE` exported at _lib.sh **top level** (NOT inside `fake_bin`, which runs in a `$(...)` subshell where the export is lost). Live agent-browser suites flake when run back-to-back without `run-all.sh`'s teardown (shared global daemon) — run isolated + `agent-browser.sh reset` to confirm a real failure vs a daemon-sharing flake.
- **agent-browser daemon/port gotchas (in `.agent0/memory/agent-browser-primitive.md`):** `close --all` HANGS with no daemon (wrapper `reset` guards it); global daemon ignores `--profile`/`--state` if up (`reset` or isolated `--session`); kill by argv0 (`agent-browser-linux*`), NEVER `pkill -f agent-browser` (self-kill, exit 144). JSON envelope `{success,data,error}`; `eval --json` → `.data.result`. Pinned 0.27.1.
- **Routing is now fail-closed (spec 153 — reverses 152's permanent fallback):** `route` → `primary` | `unavailable:{no-binary,no-chrome,mcp-removed}`; explicit commands fail closed (rc 4; rc 3 for the removed mcp override). NO MCP lane in the wrapper. The `08-no-mcp-coupling.sh` grep-guard + the squad gate's inline grep prevent reintroduction.
- **`/squad` operational lessons — now FIXED by spec 154 (`2392590`):** (1) the `squad.json.example` `forbidden_paths` `"secrets"` was too broad (matched `secrets-scan.md`) → anchored to `(^|/)secrets?/` + `\.secrets?(\.[^/]+)?$`; (2) the session-handoff Stop-nag made `codex/claude exec` peer turns rewrite the forbidden `HANDOFF.md` → the bridges now export `CLAUDE_SKIP_SESSION_HOOKS=1` so a bounded subprocess never nags; (3) out-of-turn edits tripping `aborted_conflict` with no clean recovery → `squad.sh resume` re-baselines non-destructively (the manual no-op-turn workaround is no longer needed). See `rules/squad.md` § Shape + `squad-contract.md` § Recovery.
- **Removing a `.gitignore` line for a dir that still holds gitignored credential files UN-hides them** (they become untracked/stageable) — keep the retired `.browser-state/` dir gitignored until it's physically deleted.
- **Site maintenance toggle is one flag, source-preserving:** `site/src/config.ts` `UNDER_CONSTRUCTION` (default `true`). Layouts (`Landing`/`Doc`) + root `index.astro` render `Maintenance.astro` when on; the real page markup lives untouched in the `else` branch (`<!doctype html>` kept once at top, then a `{flag ? <Maintenance/> : (<html>…)}` ternary). The `check-currency.ts` prebuild guard still runs and passes regardless of the flag. Don't delete any site content — the founder constraint was explicit ("não excluir nada").
